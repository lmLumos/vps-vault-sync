/*
THIS IS A GENERATED/BUNDLED FILE BY ESBUILD
if you want to view the source, please visit the github repository
*/

"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __commonJS = (cb, mod) => function __require() {
  return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
};
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var __publicField = (obj, key, value) => {
  __defNormalProp(obj, typeof key !== "symbol" ? key + "" : key, value);
  return value;
};

// ../shared/dist/types.js
var require_types = __commonJS({
  "../shared/dist/types.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.DEFAULT_SYNC_OPTIONS = void 0;
    exports.DEFAULT_SYNC_OPTIONS = {
      syncObsidianConfig: true,
      syncWorkspace: false,
      conflictStrategy: "three-way",
      ignoredPatterns: [
        ".git/**",
        ".sync-archive/**",
        ".sync-trash/**",
        ".trash/**",
        "**/.DS_Store",
        "**/Thumbs.db",
        "**/desktop.ini",
        "**/*.tmp",
        "**/*.swp",
        "**/*~",
        ".obsidian/cache/**",
        ".obsidian/workspace.json.tmp"
      ]
    };
  }
});

// ../shared/dist/protocol.js
var require_protocol = __commonJS({
  "../shared/dist/protocol.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.PROTOCOL_VERSION = void 0;
    exports.PROTOCOL_VERSION = "1.0.0";
  }
});

// ../shared/dist/hash.js
var require_hash = __commonJS({
  "../shared/dist/hash.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.hashString = hashString3;
    exports.hashBuffer = hashBuffer3;
    exports.isBinaryFile = isBinaryFile3;
    function sha256Sync(data) {
      const K = [
        1116352408,
        1899447441,
        3049323471,
        3921009573,
        961987163,
        1508970993,
        2453635748,
        2870763221,
        3624381080,
        310598401,
        607225278,
        1426881987,
        1925078388,
        2162078206,
        2614888103,
        3248222580,
        3835390401,
        4022224774,
        264347078,
        604807628,
        770255983,
        1249150122,
        1555081692,
        1996064986,
        2554220882,
        2821834349,
        2952996808,
        3210313671,
        3336571891,
        3584528711,
        113926993,
        338241895,
        666307205,
        773529912,
        1294757372,
        1396182291,
        1695183700,
        1986661051,
        2177026350,
        2456956037,
        2730485921,
        2820302411,
        3259730800,
        3345764771,
        3516065817,
        3600352804,
        4094571909,
        275423344,
        430227734,
        506948616,
        659060556,
        883997877,
        958139571,
        1322822218,
        1537002063,
        1747873779,
        1955562222,
        2024104815,
        2227730452,
        2361852424,
        2428436474,
        2756734187,
        3204031479,
        3329325298
      ];
      let H0 = 1779033703, H1 = 3144134277, H2 = 1013904242, H3 = 2773480762;
      let H4 = 1359893119, H5 = 2600822924, H6 = 528734635, H7 = 1541459225;
      const len = data.length;
      const bitLen = len * 8;
      const padLen = (len + 8 >> 6) + 1 << 6;
      const padded = new Uint8Array(padLen);
      padded.set(data);
      padded[len] = 128;
      const view = new DataView(padded.buffer);
      view.setUint32(padLen - 4, bitLen, false);
      const W = new Uint32Array(64);
      for (let i = 0; i < padLen; i += 64) {
        for (let t = 0; t < 16; t++) {
          W[t] = view.getUint32(i + t * 4, false);
        }
        for (let t = 16; t < 64; t++) {
          const s0 = (W[t - 15] >>> 7 | W[t - 15] << 25) ^ (W[t - 15] >>> 18 | W[t - 15] << 14) ^ W[t - 15] >>> 3;
          const s1 = (W[t - 2] >>> 17 | W[t - 2] << 15) ^ (W[t - 2] >>> 19 | W[t - 2] << 13) ^ W[t - 2] >>> 10;
          W[t] = W[t - 16] + s0 + W[t - 7] + s1 | 0;
        }
        let a = H0, b = H1, c = H2, d = H3, e = H4, f = H5, g = H6, h = H7;
        for (let t = 0; t < 64; t++) {
          const S1 = (e >>> 6 | e << 26) ^ (e >>> 11 | e << 21) ^ (e >>> 25 | e << 7);
          const ch = e & f ^ ~e & g;
          const temp1 = h + S1 + ch + K[t] + W[t] | 0;
          const S0 = (a >>> 2 | a << 30) ^ (a >>> 13 | a << 19) ^ (a >>> 22 | a << 10);
          const maj = a & b ^ a & c ^ b & c;
          const temp2 = S0 + maj | 0;
          h = g;
          g = f;
          f = e;
          e = d + temp1 | 0;
          d = c;
          c = b;
          b = a;
          a = temp1 + temp2 | 0;
        }
        H0 = H0 + a | 0;
        H1 = H1 + b | 0;
        H2 = H2 + c | 0;
        H3 = H3 + d | 0;
        H4 = H4 + e | 0;
        H5 = H5 + f | 0;
        H6 = H6 + g | 0;
        H7 = H7 + h | 0;
      }
      const toHex = (n) => (n >>> 0).toString(16).padStart(8, "0");
      return `${toHex(H0)}${toHex(H1)}${toHex(H2)}${toHex(H3)}${toHex(H4)}${toHex(H5)}${toHex(H6)}${toHex(H7)}`;
    }
    function hashString3(str) {
      const encoder = new TextEncoder();
      return sha256Sync(encoder.encode(str));
    }
    function hashBuffer3(buffer) {
      const u8 = buffer instanceof Uint8Array ? buffer : new Uint8Array(buffer);
      return sha256Sync(u8);
    }
    function isBinaryFile3(path) {
      const binaryExtensions = /* @__PURE__ */ new Set([
        "png",
        "jpg",
        "jpeg",
        "gif",
        "webp",
        "bmp",
        "svg",
        "ico",
        "pdf",
        "epub",
        "mobi",
        "mp3",
        "wav",
        "ogg",
        "m4a",
        "flac",
        "aac",
        "mp4",
        "webm",
        "mov",
        "avi",
        "mkv",
        "zip",
        "tar",
        "gz",
        "7z",
        "rar",
        "woff",
        "woff2",
        "ttf",
        "eot",
        "otf",
        "exe",
        "dylib",
        "so",
        "bin"
      ]);
      const ext = path.split(".").pop()?.toLowerCase() || "";
      return binaryExtensions.has(ext);
    }
  }
});

// ../../node_modules/diff-match-patch/index.js
var require_diff_match_patch = __commonJS({
  "../../node_modules/diff-match-patch/index.js"(exports, module2) {
    var diff_match_patch = function() {
      this.Diff_Timeout = 1;
      this.Diff_EditCost = 4;
      this.Match_Threshold = 0.5;
      this.Match_Distance = 1e3;
      this.Patch_DeleteThreshold = 0.5;
      this.Patch_Margin = 4;
      this.Match_MaxBits = 32;
    };
    var DIFF_DELETE = -1;
    var DIFF_INSERT = 1;
    var DIFF_EQUAL = 0;
    diff_match_patch.Diff = function(op, text) {
      return [op, text];
    };
    diff_match_patch.prototype.diff_main = function(text1, text2, opt_checklines, opt_deadline) {
      if (typeof opt_deadline == "undefined") {
        if (this.Diff_Timeout <= 0) {
          opt_deadline = Number.MAX_VALUE;
        } else {
          opt_deadline = (/* @__PURE__ */ new Date()).getTime() + this.Diff_Timeout * 1e3;
        }
      }
      var deadline = opt_deadline;
      if (text1 == null || text2 == null) {
        throw new Error("Null input. (diff_main)");
      }
      if (text1 == text2) {
        if (text1) {
          return [new diff_match_patch.Diff(DIFF_EQUAL, text1)];
        }
        return [];
      }
      if (typeof opt_checklines == "undefined") {
        opt_checklines = true;
      }
      var checklines = opt_checklines;
      var commonlength = this.diff_commonPrefix(text1, text2);
      var commonprefix = text1.substring(0, commonlength);
      text1 = text1.substring(commonlength);
      text2 = text2.substring(commonlength);
      commonlength = this.diff_commonSuffix(text1, text2);
      var commonsuffix = text1.substring(text1.length - commonlength);
      text1 = text1.substring(0, text1.length - commonlength);
      text2 = text2.substring(0, text2.length - commonlength);
      var diffs = this.diff_compute_(text1, text2, checklines, deadline);
      if (commonprefix) {
        diffs.unshift(new diff_match_patch.Diff(DIFF_EQUAL, commonprefix));
      }
      if (commonsuffix) {
        diffs.push(new diff_match_patch.Diff(DIFF_EQUAL, commonsuffix));
      }
      this.diff_cleanupMerge(diffs);
      return diffs;
    };
    diff_match_patch.prototype.diff_compute_ = function(text1, text2, checklines, deadline) {
      var diffs;
      if (!text1) {
        return [new diff_match_patch.Diff(DIFF_INSERT, text2)];
      }
      if (!text2) {
        return [new diff_match_patch.Diff(DIFF_DELETE, text1)];
      }
      var longtext = text1.length > text2.length ? text1 : text2;
      var shorttext = text1.length > text2.length ? text2 : text1;
      var i = longtext.indexOf(shorttext);
      if (i != -1) {
        diffs = [
          new diff_match_patch.Diff(DIFF_INSERT, longtext.substring(0, i)),
          new diff_match_patch.Diff(DIFF_EQUAL, shorttext),
          new diff_match_patch.Diff(
            DIFF_INSERT,
            longtext.substring(i + shorttext.length)
          )
        ];
        if (text1.length > text2.length) {
          diffs[0][0] = diffs[2][0] = DIFF_DELETE;
        }
        return diffs;
      }
      if (shorttext.length == 1) {
        return [
          new diff_match_patch.Diff(DIFF_DELETE, text1),
          new diff_match_patch.Diff(DIFF_INSERT, text2)
        ];
      }
      var hm = this.diff_halfMatch_(text1, text2);
      if (hm) {
        var text1_a = hm[0];
        var text1_b = hm[1];
        var text2_a = hm[2];
        var text2_b = hm[3];
        var mid_common = hm[4];
        var diffs_a = this.diff_main(text1_a, text2_a, checklines, deadline);
        var diffs_b = this.diff_main(text1_b, text2_b, checklines, deadline);
        return diffs_a.concat(
          [new diff_match_patch.Diff(DIFF_EQUAL, mid_common)],
          diffs_b
        );
      }
      if (checklines && text1.length > 100 && text2.length > 100) {
        return this.diff_lineMode_(text1, text2, deadline);
      }
      return this.diff_bisect_(text1, text2, deadline);
    };
    diff_match_patch.prototype.diff_lineMode_ = function(text1, text2, deadline) {
      var a = this.diff_linesToChars_(text1, text2);
      text1 = a.chars1;
      text2 = a.chars2;
      var linearray = a.lineArray;
      var diffs = this.diff_main(text1, text2, false, deadline);
      this.diff_charsToLines_(diffs, linearray);
      this.diff_cleanupSemantic(diffs);
      diffs.push(new diff_match_patch.Diff(DIFF_EQUAL, ""));
      var pointer = 0;
      var count_delete = 0;
      var count_insert = 0;
      var text_delete = "";
      var text_insert = "";
      while (pointer < diffs.length) {
        switch (diffs[pointer][0]) {
          case DIFF_INSERT:
            count_insert++;
            text_insert += diffs[pointer][1];
            break;
          case DIFF_DELETE:
            count_delete++;
            text_delete += diffs[pointer][1];
            break;
          case DIFF_EQUAL:
            if (count_delete >= 1 && count_insert >= 1) {
              diffs.splice(
                pointer - count_delete - count_insert,
                count_delete + count_insert
              );
              pointer = pointer - count_delete - count_insert;
              var subDiff = this.diff_main(text_delete, text_insert, false, deadline);
              for (var j = subDiff.length - 1; j >= 0; j--) {
                diffs.splice(pointer, 0, subDiff[j]);
              }
              pointer = pointer + subDiff.length;
            }
            count_insert = 0;
            count_delete = 0;
            text_delete = "";
            text_insert = "";
            break;
        }
        pointer++;
      }
      diffs.pop();
      return diffs;
    };
    diff_match_patch.prototype.diff_bisect_ = function(text1, text2, deadline) {
      var text1_length = text1.length;
      var text2_length = text2.length;
      var max_d = Math.ceil((text1_length + text2_length) / 2);
      var v_offset = max_d;
      var v_length = 2 * max_d;
      var v1 = new Array(v_length);
      var v2 = new Array(v_length);
      for (var x = 0; x < v_length; x++) {
        v1[x] = -1;
        v2[x] = -1;
      }
      v1[v_offset + 1] = 0;
      v2[v_offset + 1] = 0;
      var delta = text1_length - text2_length;
      var front = delta % 2 != 0;
      var k1start = 0;
      var k1end = 0;
      var k2start = 0;
      var k2end = 0;
      for (var d = 0; d < max_d; d++) {
        if ((/* @__PURE__ */ new Date()).getTime() > deadline) {
          break;
        }
        for (var k1 = -d + k1start; k1 <= d - k1end; k1 += 2) {
          var k1_offset = v_offset + k1;
          var x1;
          if (k1 == -d || k1 != d && v1[k1_offset - 1] < v1[k1_offset + 1]) {
            x1 = v1[k1_offset + 1];
          } else {
            x1 = v1[k1_offset - 1] + 1;
          }
          var y1 = x1 - k1;
          while (x1 < text1_length && y1 < text2_length && text1.charAt(x1) == text2.charAt(y1)) {
            x1++;
            y1++;
          }
          v1[k1_offset] = x1;
          if (x1 > text1_length) {
            k1end += 2;
          } else if (y1 > text2_length) {
            k1start += 2;
          } else if (front) {
            var k2_offset = v_offset + delta - k1;
            if (k2_offset >= 0 && k2_offset < v_length && v2[k2_offset] != -1) {
              var x2 = text1_length - v2[k2_offset];
              if (x1 >= x2) {
                return this.diff_bisectSplit_(text1, text2, x1, y1, deadline);
              }
            }
          }
        }
        for (var k2 = -d + k2start; k2 <= d - k2end; k2 += 2) {
          var k2_offset = v_offset + k2;
          var x2;
          if (k2 == -d || k2 != d && v2[k2_offset - 1] < v2[k2_offset + 1]) {
            x2 = v2[k2_offset + 1];
          } else {
            x2 = v2[k2_offset - 1] + 1;
          }
          var y2 = x2 - k2;
          while (x2 < text1_length && y2 < text2_length && text1.charAt(text1_length - x2 - 1) == text2.charAt(text2_length - y2 - 1)) {
            x2++;
            y2++;
          }
          v2[k2_offset] = x2;
          if (x2 > text1_length) {
            k2end += 2;
          } else if (y2 > text2_length) {
            k2start += 2;
          } else if (!front) {
            var k1_offset = v_offset + delta - k2;
            if (k1_offset >= 0 && k1_offset < v_length && v1[k1_offset] != -1) {
              var x1 = v1[k1_offset];
              var y1 = v_offset + x1 - k1_offset;
              x2 = text1_length - x2;
              if (x1 >= x2) {
                return this.diff_bisectSplit_(text1, text2, x1, y1, deadline);
              }
            }
          }
        }
      }
      return [
        new diff_match_patch.Diff(DIFF_DELETE, text1),
        new diff_match_patch.Diff(DIFF_INSERT, text2)
      ];
    };
    diff_match_patch.prototype.diff_bisectSplit_ = function(text1, text2, x, y, deadline) {
      var text1a = text1.substring(0, x);
      var text2a = text2.substring(0, y);
      var text1b = text1.substring(x);
      var text2b = text2.substring(y);
      var diffs = this.diff_main(text1a, text2a, false, deadline);
      var diffsb = this.diff_main(text1b, text2b, false, deadline);
      return diffs.concat(diffsb);
    };
    diff_match_patch.prototype.diff_linesToChars_ = function(text1, text2) {
      var lineArray = [];
      var lineHash = {};
      lineArray[0] = "";
      function diff_linesToCharsMunge_(text) {
        var chars = "";
        var lineStart = 0;
        var lineEnd = -1;
        var lineArrayLength = lineArray.length;
        while (lineEnd < text.length - 1) {
          lineEnd = text.indexOf("\n", lineStart);
          if (lineEnd == -1) {
            lineEnd = text.length - 1;
          }
          var line = text.substring(lineStart, lineEnd + 1);
          if (lineHash.hasOwnProperty ? lineHash.hasOwnProperty(line) : lineHash[line] !== void 0) {
            chars += String.fromCharCode(lineHash[line]);
          } else {
            if (lineArrayLength == maxLines) {
              line = text.substring(lineStart);
              lineEnd = text.length;
            }
            chars += String.fromCharCode(lineArrayLength);
            lineHash[line] = lineArrayLength;
            lineArray[lineArrayLength++] = line;
          }
          lineStart = lineEnd + 1;
        }
        return chars;
      }
      var maxLines = 4e4;
      var chars1 = diff_linesToCharsMunge_(text1);
      maxLines = 65535;
      var chars2 = diff_linesToCharsMunge_(text2);
      return { chars1, chars2, lineArray };
    };
    diff_match_patch.prototype.diff_charsToLines_ = function(diffs, lineArray) {
      for (var i = 0; i < diffs.length; i++) {
        var chars = diffs[i][1];
        var text = [];
        for (var j = 0; j < chars.length; j++) {
          text[j] = lineArray[chars.charCodeAt(j)];
        }
        diffs[i][1] = text.join("");
      }
    };
    diff_match_patch.prototype.diff_commonPrefix = function(text1, text2) {
      if (!text1 || !text2 || text1.charAt(0) != text2.charAt(0)) {
        return 0;
      }
      var pointermin = 0;
      var pointermax = Math.min(text1.length, text2.length);
      var pointermid = pointermax;
      var pointerstart = 0;
      while (pointermin < pointermid) {
        if (text1.substring(pointerstart, pointermid) == text2.substring(pointerstart, pointermid)) {
          pointermin = pointermid;
          pointerstart = pointermin;
        } else {
          pointermax = pointermid;
        }
        pointermid = Math.floor((pointermax - pointermin) / 2 + pointermin);
      }
      return pointermid;
    };
    diff_match_patch.prototype.diff_commonSuffix = function(text1, text2) {
      if (!text1 || !text2 || text1.charAt(text1.length - 1) != text2.charAt(text2.length - 1)) {
        return 0;
      }
      var pointermin = 0;
      var pointermax = Math.min(text1.length, text2.length);
      var pointermid = pointermax;
      var pointerend = 0;
      while (pointermin < pointermid) {
        if (text1.substring(text1.length - pointermid, text1.length - pointerend) == text2.substring(text2.length - pointermid, text2.length - pointerend)) {
          pointermin = pointermid;
          pointerend = pointermin;
        } else {
          pointermax = pointermid;
        }
        pointermid = Math.floor((pointermax - pointermin) / 2 + pointermin);
      }
      return pointermid;
    };
    diff_match_patch.prototype.diff_commonOverlap_ = function(text1, text2) {
      var text1_length = text1.length;
      var text2_length = text2.length;
      if (text1_length == 0 || text2_length == 0) {
        return 0;
      }
      if (text1_length > text2_length) {
        text1 = text1.substring(text1_length - text2_length);
      } else if (text1_length < text2_length) {
        text2 = text2.substring(0, text1_length);
      }
      var text_length = Math.min(text1_length, text2_length);
      if (text1 == text2) {
        return text_length;
      }
      var best = 0;
      var length = 1;
      while (true) {
        var pattern = text1.substring(text_length - length);
        var found = text2.indexOf(pattern);
        if (found == -1) {
          return best;
        }
        length += found;
        if (found == 0 || text1.substring(text_length - length) == text2.substring(0, length)) {
          best = length;
          length++;
        }
      }
    };
    diff_match_patch.prototype.diff_halfMatch_ = function(text1, text2) {
      if (this.Diff_Timeout <= 0) {
        return null;
      }
      var longtext = text1.length > text2.length ? text1 : text2;
      var shorttext = text1.length > text2.length ? text2 : text1;
      if (longtext.length < 4 || shorttext.length * 2 < longtext.length) {
        return null;
      }
      var dmp = this;
      function diff_halfMatchI_(longtext2, shorttext2, i) {
        var seed = longtext2.substring(i, i + Math.floor(longtext2.length / 4));
        var j = -1;
        var best_common = "";
        var best_longtext_a, best_longtext_b, best_shorttext_a, best_shorttext_b;
        while ((j = shorttext2.indexOf(seed, j + 1)) != -1) {
          var prefixLength = dmp.diff_commonPrefix(
            longtext2.substring(i),
            shorttext2.substring(j)
          );
          var suffixLength = dmp.diff_commonSuffix(
            longtext2.substring(0, i),
            shorttext2.substring(0, j)
          );
          if (best_common.length < suffixLength + prefixLength) {
            best_common = shorttext2.substring(j - suffixLength, j) + shorttext2.substring(j, j + prefixLength);
            best_longtext_a = longtext2.substring(0, i - suffixLength);
            best_longtext_b = longtext2.substring(i + prefixLength);
            best_shorttext_a = shorttext2.substring(0, j - suffixLength);
            best_shorttext_b = shorttext2.substring(j + prefixLength);
          }
        }
        if (best_common.length * 2 >= longtext2.length) {
          return [
            best_longtext_a,
            best_longtext_b,
            best_shorttext_a,
            best_shorttext_b,
            best_common
          ];
        } else {
          return null;
        }
      }
      var hm1 = diff_halfMatchI_(
        longtext,
        shorttext,
        Math.ceil(longtext.length / 4)
      );
      var hm2 = diff_halfMatchI_(
        longtext,
        shorttext,
        Math.ceil(longtext.length / 2)
      );
      var hm;
      if (!hm1 && !hm2) {
        return null;
      } else if (!hm2) {
        hm = hm1;
      } else if (!hm1) {
        hm = hm2;
      } else {
        hm = hm1[4].length > hm2[4].length ? hm1 : hm2;
      }
      var text1_a, text1_b, text2_a, text2_b;
      if (text1.length > text2.length) {
        text1_a = hm[0];
        text1_b = hm[1];
        text2_a = hm[2];
        text2_b = hm[3];
      } else {
        text2_a = hm[0];
        text2_b = hm[1];
        text1_a = hm[2];
        text1_b = hm[3];
      }
      var mid_common = hm[4];
      return [text1_a, text1_b, text2_a, text2_b, mid_common];
    };
    diff_match_patch.prototype.diff_cleanupSemantic = function(diffs) {
      var changes = false;
      var equalities = [];
      var equalitiesLength = 0;
      var lastEquality = null;
      var pointer = 0;
      var length_insertions1 = 0;
      var length_deletions1 = 0;
      var length_insertions2 = 0;
      var length_deletions2 = 0;
      while (pointer < diffs.length) {
        if (diffs[pointer][0] == DIFF_EQUAL) {
          equalities[equalitiesLength++] = pointer;
          length_insertions1 = length_insertions2;
          length_deletions1 = length_deletions2;
          length_insertions2 = 0;
          length_deletions2 = 0;
          lastEquality = diffs[pointer][1];
        } else {
          if (diffs[pointer][0] == DIFF_INSERT) {
            length_insertions2 += diffs[pointer][1].length;
          } else {
            length_deletions2 += diffs[pointer][1].length;
          }
          if (lastEquality && lastEquality.length <= Math.max(length_insertions1, length_deletions1) && lastEquality.length <= Math.max(
            length_insertions2,
            length_deletions2
          )) {
            diffs.splice(
              equalities[equalitiesLength - 1],
              0,
              new diff_match_patch.Diff(DIFF_DELETE, lastEquality)
            );
            diffs[equalities[equalitiesLength - 1] + 1][0] = DIFF_INSERT;
            equalitiesLength--;
            equalitiesLength--;
            pointer = equalitiesLength > 0 ? equalities[equalitiesLength - 1] : -1;
            length_insertions1 = 0;
            length_deletions1 = 0;
            length_insertions2 = 0;
            length_deletions2 = 0;
            lastEquality = null;
            changes = true;
          }
        }
        pointer++;
      }
      if (changes) {
        this.diff_cleanupMerge(diffs);
      }
      this.diff_cleanupSemanticLossless(diffs);
      pointer = 1;
      while (pointer < diffs.length) {
        if (diffs[pointer - 1][0] == DIFF_DELETE && diffs[pointer][0] == DIFF_INSERT) {
          var deletion = diffs[pointer - 1][1];
          var insertion = diffs[pointer][1];
          var overlap_length1 = this.diff_commonOverlap_(deletion, insertion);
          var overlap_length2 = this.diff_commonOverlap_(insertion, deletion);
          if (overlap_length1 >= overlap_length2) {
            if (overlap_length1 >= deletion.length / 2 || overlap_length1 >= insertion.length / 2) {
              diffs.splice(pointer, 0, new diff_match_patch.Diff(
                DIFF_EQUAL,
                insertion.substring(0, overlap_length1)
              ));
              diffs[pointer - 1][1] = deletion.substring(0, deletion.length - overlap_length1);
              diffs[pointer + 1][1] = insertion.substring(overlap_length1);
              pointer++;
            }
          } else {
            if (overlap_length2 >= deletion.length / 2 || overlap_length2 >= insertion.length / 2) {
              diffs.splice(pointer, 0, new diff_match_patch.Diff(
                DIFF_EQUAL,
                deletion.substring(0, overlap_length2)
              ));
              diffs[pointer - 1][0] = DIFF_INSERT;
              diffs[pointer - 1][1] = insertion.substring(0, insertion.length - overlap_length2);
              diffs[pointer + 1][0] = DIFF_DELETE;
              diffs[pointer + 1][1] = deletion.substring(overlap_length2);
              pointer++;
            }
          }
          pointer++;
        }
        pointer++;
      }
    };
    diff_match_patch.prototype.diff_cleanupSemanticLossless = function(diffs) {
      function diff_cleanupSemanticScore_(one, two) {
        if (!one || !two) {
          return 6;
        }
        var char1 = one.charAt(one.length - 1);
        var char2 = two.charAt(0);
        var nonAlphaNumeric1 = char1.match(diff_match_patch.nonAlphaNumericRegex_);
        var nonAlphaNumeric2 = char2.match(diff_match_patch.nonAlphaNumericRegex_);
        var whitespace1 = nonAlphaNumeric1 && char1.match(diff_match_patch.whitespaceRegex_);
        var whitespace2 = nonAlphaNumeric2 && char2.match(diff_match_patch.whitespaceRegex_);
        var lineBreak1 = whitespace1 && char1.match(diff_match_patch.linebreakRegex_);
        var lineBreak2 = whitespace2 && char2.match(diff_match_patch.linebreakRegex_);
        var blankLine1 = lineBreak1 && one.match(diff_match_patch.blanklineEndRegex_);
        var blankLine2 = lineBreak2 && two.match(diff_match_patch.blanklineStartRegex_);
        if (blankLine1 || blankLine2) {
          return 5;
        } else if (lineBreak1 || lineBreak2) {
          return 4;
        } else if (nonAlphaNumeric1 && !whitespace1 && whitespace2) {
          return 3;
        } else if (whitespace1 || whitespace2) {
          return 2;
        } else if (nonAlphaNumeric1 || nonAlphaNumeric2) {
          return 1;
        }
        return 0;
      }
      var pointer = 1;
      while (pointer < diffs.length - 1) {
        if (diffs[pointer - 1][0] == DIFF_EQUAL && diffs[pointer + 1][0] == DIFF_EQUAL) {
          var equality1 = diffs[pointer - 1][1];
          var edit = diffs[pointer][1];
          var equality2 = diffs[pointer + 1][1];
          var commonOffset = this.diff_commonSuffix(equality1, edit);
          if (commonOffset) {
            var commonString = edit.substring(edit.length - commonOffset);
            equality1 = equality1.substring(0, equality1.length - commonOffset);
            edit = commonString + edit.substring(0, edit.length - commonOffset);
            equality2 = commonString + equality2;
          }
          var bestEquality1 = equality1;
          var bestEdit = edit;
          var bestEquality2 = equality2;
          var bestScore = diff_cleanupSemanticScore_(equality1, edit) + diff_cleanupSemanticScore_(edit, equality2);
          while (edit.charAt(0) === equality2.charAt(0)) {
            equality1 += edit.charAt(0);
            edit = edit.substring(1) + equality2.charAt(0);
            equality2 = equality2.substring(1);
            var score = diff_cleanupSemanticScore_(equality1, edit) + diff_cleanupSemanticScore_(edit, equality2);
            if (score >= bestScore) {
              bestScore = score;
              bestEquality1 = equality1;
              bestEdit = edit;
              bestEquality2 = equality2;
            }
          }
          if (diffs[pointer - 1][1] != bestEquality1) {
            if (bestEquality1) {
              diffs[pointer - 1][1] = bestEquality1;
            } else {
              diffs.splice(pointer - 1, 1);
              pointer--;
            }
            diffs[pointer][1] = bestEdit;
            if (bestEquality2) {
              diffs[pointer + 1][1] = bestEquality2;
            } else {
              diffs.splice(pointer + 1, 1);
              pointer--;
            }
          }
        }
        pointer++;
      }
    };
    diff_match_patch.nonAlphaNumericRegex_ = /[^a-zA-Z0-9]/;
    diff_match_patch.whitespaceRegex_ = /\s/;
    diff_match_patch.linebreakRegex_ = /[\r\n]/;
    diff_match_patch.blanklineEndRegex_ = /\n\r?\n$/;
    diff_match_patch.blanklineStartRegex_ = /^\r?\n\r?\n/;
    diff_match_patch.prototype.diff_cleanupEfficiency = function(diffs) {
      var changes = false;
      var equalities = [];
      var equalitiesLength = 0;
      var lastEquality = null;
      var pointer = 0;
      var pre_ins = false;
      var pre_del = false;
      var post_ins = false;
      var post_del = false;
      while (pointer < diffs.length) {
        if (diffs[pointer][0] == DIFF_EQUAL) {
          if (diffs[pointer][1].length < this.Diff_EditCost && (post_ins || post_del)) {
            equalities[equalitiesLength++] = pointer;
            pre_ins = post_ins;
            pre_del = post_del;
            lastEquality = diffs[pointer][1];
          } else {
            equalitiesLength = 0;
            lastEquality = null;
          }
          post_ins = post_del = false;
        } else {
          if (diffs[pointer][0] == DIFF_DELETE) {
            post_del = true;
          } else {
            post_ins = true;
          }
          if (lastEquality && (pre_ins && pre_del && post_ins && post_del || lastEquality.length < this.Diff_EditCost / 2 && pre_ins + pre_del + post_ins + post_del == 3)) {
            diffs.splice(
              equalities[equalitiesLength - 1],
              0,
              new diff_match_patch.Diff(DIFF_DELETE, lastEquality)
            );
            diffs[equalities[equalitiesLength - 1] + 1][0] = DIFF_INSERT;
            equalitiesLength--;
            lastEquality = null;
            if (pre_ins && pre_del) {
              post_ins = post_del = true;
              equalitiesLength = 0;
            } else {
              equalitiesLength--;
              pointer = equalitiesLength > 0 ? equalities[equalitiesLength - 1] : -1;
              post_ins = post_del = false;
            }
            changes = true;
          }
        }
        pointer++;
      }
      if (changes) {
        this.diff_cleanupMerge(diffs);
      }
    };
    diff_match_patch.prototype.diff_cleanupMerge = function(diffs) {
      diffs.push(new diff_match_patch.Diff(DIFF_EQUAL, ""));
      var pointer = 0;
      var count_delete = 0;
      var count_insert = 0;
      var text_delete = "";
      var text_insert = "";
      var commonlength;
      while (pointer < diffs.length) {
        switch (diffs[pointer][0]) {
          case DIFF_INSERT:
            count_insert++;
            text_insert += diffs[pointer][1];
            pointer++;
            break;
          case DIFF_DELETE:
            count_delete++;
            text_delete += diffs[pointer][1];
            pointer++;
            break;
          case DIFF_EQUAL:
            if (count_delete + count_insert > 1) {
              if (count_delete !== 0 && count_insert !== 0) {
                commonlength = this.diff_commonPrefix(text_insert, text_delete);
                if (commonlength !== 0) {
                  if (pointer - count_delete - count_insert > 0 && diffs[pointer - count_delete - count_insert - 1][0] == DIFF_EQUAL) {
                    diffs[pointer - count_delete - count_insert - 1][1] += text_insert.substring(0, commonlength);
                  } else {
                    diffs.splice(0, 0, new diff_match_patch.Diff(
                      DIFF_EQUAL,
                      text_insert.substring(0, commonlength)
                    ));
                    pointer++;
                  }
                  text_insert = text_insert.substring(commonlength);
                  text_delete = text_delete.substring(commonlength);
                }
                commonlength = this.diff_commonSuffix(text_insert, text_delete);
                if (commonlength !== 0) {
                  diffs[pointer][1] = text_insert.substring(text_insert.length - commonlength) + diffs[pointer][1];
                  text_insert = text_insert.substring(0, text_insert.length - commonlength);
                  text_delete = text_delete.substring(0, text_delete.length - commonlength);
                }
              }
              pointer -= count_delete + count_insert;
              diffs.splice(pointer, count_delete + count_insert);
              if (text_delete.length) {
                diffs.splice(
                  pointer,
                  0,
                  new diff_match_patch.Diff(DIFF_DELETE, text_delete)
                );
                pointer++;
              }
              if (text_insert.length) {
                diffs.splice(
                  pointer,
                  0,
                  new diff_match_patch.Diff(DIFF_INSERT, text_insert)
                );
                pointer++;
              }
              pointer++;
            } else if (pointer !== 0 && diffs[pointer - 1][0] == DIFF_EQUAL) {
              diffs[pointer - 1][1] += diffs[pointer][1];
              diffs.splice(pointer, 1);
            } else {
              pointer++;
            }
            count_insert = 0;
            count_delete = 0;
            text_delete = "";
            text_insert = "";
            break;
        }
      }
      if (diffs[diffs.length - 1][1] === "") {
        diffs.pop();
      }
      var changes = false;
      pointer = 1;
      while (pointer < diffs.length - 1) {
        if (diffs[pointer - 1][0] == DIFF_EQUAL && diffs[pointer + 1][0] == DIFF_EQUAL) {
          if (diffs[pointer][1].substring(diffs[pointer][1].length - diffs[pointer - 1][1].length) == diffs[pointer - 1][1]) {
            diffs[pointer][1] = diffs[pointer - 1][1] + diffs[pointer][1].substring(0, diffs[pointer][1].length - diffs[pointer - 1][1].length);
            diffs[pointer + 1][1] = diffs[pointer - 1][1] + diffs[pointer + 1][1];
            diffs.splice(pointer - 1, 1);
            changes = true;
          } else if (diffs[pointer][1].substring(0, diffs[pointer + 1][1].length) == diffs[pointer + 1][1]) {
            diffs[pointer - 1][1] += diffs[pointer + 1][1];
            diffs[pointer][1] = diffs[pointer][1].substring(diffs[pointer + 1][1].length) + diffs[pointer + 1][1];
            diffs.splice(pointer + 1, 1);
            changes = true;
          }
        }
        pointer++;
      }
      if (changes) {
        this.diff_cleanupMerge(diffs);
      }
    };
    diff_match_patch.prototype.diff_xIndex = function(diffs, loc) {
      var chars1 = 0;
      var chars2 = 0;
      var last_chars1 = 0;
      var last_chars2 = 0;
      var x;
      for (x = 0; x < diffs.length; x++) {
        if (diffs[x][0] !== DIFF_INSERT) {
          chars1 += diffs[x][1].length;
        }
        if (diffs[x][0] !== DIFF_DELETE) {
          chars2 += diffs[x][1].length;
        }
        if (chars1 > loc) {
          break;
        }
        last_chars1 = chars1;
        last_chars2 = chars2;
      }
      if (diffs.length != x && diffs[x][0] === DIFF_DELETE) {
        return last_chars2;
      }
      return last_chars2 + (loc - last_chars1);
    };
    diff_match_patch.prototype.diff_prettyHtml = function(diffs) {
      var html = [];
      var pattern_amp = /&/g;
      var pattern_lt = /</g;
      var pattern_gt = />/g;
      var pattern_para = /\n/g;
      for (var x = 0; x < diffs.length; x++) {
        var op = diffs[x][0];
        var data = diffs[x][1];
        var text = data.replace(pattern_amp, "&amp;").replace(pattern_lt, "&lt;").replace(pattern_gt, "&gt;").replace(pattern_para, "&para;<br>");
        switch (op) {
          case DIFF_INSERT:
            html[x] = '<ins style="background:#e6ffe6;">' + text + "</ins>";
            break;
          case DIFF_DELETE:
            html[x] = '<del style="background:#ffe6e6;">' + text + "</del>";
            break;
          case DIFF_EQUAL:
            html[x] = "<span>" + text + "</span>";
            break;
        }
      }
      return html.join("");
    };
    diff_match_patch.prototype.diff_text1 = function(diffs) {
      var text = [];
      for (var x = 0; x < diffs.length; x++) {
        if (diffs[x][0] !== DIFF_INSERT) {
          text[x] = diffs[x][1];
        }
      }
      return text.join("");
    };
    diff_match_patch.prototype.diff_text2 = function(diffs) {
      var text = [];
      for (var x = 0; x < diffs.length; x++) {
        if (diffs[x][0] !== DIFF_DELETE) {
          text[x] = diffs[x][1];
        }
      }
      return text.join("");
    };
    diff_match_patch.prototype.diff_levenshtein = function(diffs) {
      var levenshtein = 0;
      var insertions = 0;
      var deletions = 0;
      for (var x = 0; x < diffs.length; x++) {
        var op = diffs[x][0];
        var data = diffs[x][1];
        switch (op) {
          case DIFF_INSERT:
            insertions += data.length;
            break;
          case DIFF_DELETE:
            deletions += data.length;
            break;
          case DIFF_EQUAL:
            levenshtein += Math.max(insertions, deletions);
            insertions = 0;
            deletions = 0;
            break;
        }
      }
      levenshtein += Math.max(insertions, deletions);
      return levenshtein;
    };
    diff_match_patch.prototype.diff_toDelta = function(diffs) {
      var text = [];
      for (var x = 0; x < diffs.length; x++) {
        switch (diffs[x][0]) {
          case DIFF_INSERT:
            text[x] = "+" + encodeURI(diffs[x][1]);
            break;
          case DIFF_DELETE:
            text[x] = "-" + diffs[x][1].length;
            break;
          case DIFF_EQUAL:
            text[x] = "=" + diffs[x][1].length;
            break;
        }
      }
      return text.join("	").replace(/%20/g, " ");
    };
    diff_match_patch.prototype.diff_fromDelta = function(text1, delta) {
      var diffs = [];
      var diffsLength = 0;
      var pointer = 0;
      var tokens = delta.split(/\t/g);
      for (var x = 0; x < tokens.length; x++) {
        var param = tokens[x].substring(1);
        switch (tokens[x].charAt(0)) {
          case "+":
            try {
              diffs[diffsLength++] = new diff_match_patch.Diff(DIFF_INSERT, decodeURI(param));
            } catch (ex) {
              throw new Error("Illegal escape in diff_fromDelta: " + param);
            }
            break;
          case "-":
          case "=":
            var n = parseInt(param, 10);
            if (isNaN(n) || n < 0) {
              throw new Error("Invalid number in diff_fromDelta: " + param);
            }
            var text = text1.substring(pointer, pointer += n);
            if (tokens[x].charAt(0) == "=") {
              diffs[diffsLength++] = new diff_match_patch.Diff(DIFF_EQUAL, text);
            } else {
              diffs[diffsLength++] = new diff_match_patch.Diff(DIFF_DELETE, text);
            }
            break;
          default:
            if (tokens[x]) {
              throw new Error("Invalid diff operation in diff_fromDelta: " + tokens[x]);
            }
        }
      }
      if (pointer != text1.length) {
        throw new Error("Delta length (" + pointer + ") does not equal source text length (" + text1.length + ").");
      }
      return diffs;
    };
    diff_match_patch.prototype.match_main = function(text, pattern, loc) {
      if (text == null || pattern == null || loc == null) {
        throw new Error("Null input. (match_main)");
      }
      loc = Math.max(0, Math.min(loc, text.length));
      if (text == pattern) {
        return 0;
      } else if (!text.length) {
        return -1;
      } else if (text.substring(loc, loc + pattern.length) == pattern) {
        return loc;
      } else {
        return this.match_bitap_(text, pattern, loc);
      }
    };
    diff_match_patch.prototype.match_bitap_ = function(text, pattern, loc) {
      if (pattern.length > this.Match_MaxBits) {
        throw new Error("Pattern too long for this browser.");
      }
      var s = this.match_alphabet_(pattern);
      var dmp = this;
      function match_bitapScore_(e, x) {
        var accuracy = e / pattern.length;
        var proximity = Math.abs(loc - x);
        if (!dmp.Match_Distance) {
          return proximity ? 1 : accuracy;
        }
        return accuracy + proximity / dmp.Match_Distance;
      }
      var score_threshold = this.Match_Threshold;
      var best_loc = text.indexOf(pattern, loc);
      if (best_loc != -1) {
        score_threshold = Math.min(match_bitapScore_(0, best_loc), score_threshold);
        best_loc = text.lastIndexOf(pattern, loc + pattern.length);
        if (best_loc != -1) {
          score_threshold = Math.min(match_bitapScore_(0, best_loc), score_threshold);
        }
      }
      var matchmask = 1 << pattern.length - 1;
      best_loc = -1;
      var bin_min, bin_mid;
      var bin_max = pattern.length + text.length;
      var last_rd;
      for (var d = 0; d < pattern.length; d++) {
        bin_min = 0;
        bin_mid = bin_max;
        while (bin_min < bin_mid) {
          if (match_bitapScore_(d, loc + bin_mid) <= score_threshold) {
            bin_min = bin_mid;
          } else {
            bin_max = bin_mid;
          }
          bin_mid = Math.floor((bin_max - bin_min) / 2 + bin_min);
        }
        bin_max = bin_mid;
        var start = Math.max(1, loc - bin_mid + 1);
        var finish = Math.min(loc + bin_mid, text.length) + pattern.length;
        var rd = Array(finish + 2);
        rd[finish + 1] = (1 << d) - 1;
        for (var j = finish; j >= start; j--) {
          var charMatch = s[text.charAt(j - 1)];
          if (d === 0) {
            rd[j] = (rd[j + 1] << 1 | 1) & charMatch;
          } else {
            rd[j] = (rd[j + 1] << 1 | 1) & charMatch | ((last_rd[j + 1] | last_rd[j]) << 1 | 1) | last_rd[j + 1];
          }
          if (rd[j] & matchmask) {
            var score = match_bitapScore_(d, j - 1);
            if (score <= score_threshold) {
              score_threshold = score;
              best_loc = j - 1;
              if (best_loc > loc) {
                start = Math.max(1, 2 * loc - best_loc);
              } else {
                break;
              }
            }
          }
        }
        if (match_bitapScore_(d + 1, loc) > score_threshold) {
          break;
        }
        last_rd = rd;
      }
      return best_loc;
    };
    diff_match_patch.prototype.match_alphabet_ = function(pattern) {
      var s = {};
      for (var i = 0; i < pattern.length; i++) {
        s[pattern.charAt(i)] = 0;
      }
      for (var i = 0; i < pattern.length; i++) {
        s[pattern.charAt(i)] |= 1 << pattern.length - i - 1;
      }
      return s;
    };
    diff_match_patch.prototype.patch_addContext_ = function(patch, text) {
      if (text.length == 0) {
        return;
      }
      if (patch.start2 === null) {
        throw Error("patch not initialized");
      }
      var pattern = text.substring(patch.start2, patch.start2 + patch.length1);
      var padding = 0;
      while (text.indexOf(pattern) != text.lastIndexOf(pattern) && pattern.length < this.Match_MaxBits - this.Patch_Margin - this.Patch_Margin) {
        padding += this.Patch_Margin;
        pattern = text.substring(
          patch.start2 - padding,
          patch.start2 + patch.length1 + padding
        );
      }
      padding += this.Patch_Margin;
      var prefix = text.substring(patch.start2 - padding, patch.start2);
      if (prefix) {
        patch.diffs.unshift(new diff_match_patch.Diff(DIFF_EQUAL, prefix));
      }
      var suffix = text.substring(
        patch.start2 + patch.length1,
        patch.start2 + patch.length1 + padding
      );
      if (suffix) {
        patch.diffs.push(new diff_match_patch.Diff(DIFF_EQUAL, suffix));
      }
      patch.start1 -= prefix.length;
      patch.start2 -= prefix.length;
      patch.length1 += prefix.length + suffix.length;
      patch.length2 += prefix.length + suffix.length;
    };
    diff_match_patch.prototype.patch_make = function(a, opt_b, opt_c) {
      var text1, diffs;
      if (typeof a == "string" && typeof opt_b == "string" && typeof opt_c == "undefined") {
        text1 = /** @type {string} */
        a;
        diffs = this.diff_main(
          text1,
          /** @type {string} */
          opt_b,
          true
        );
        if (diffs.length > 2) {
          this.diff_cleanupSemantic(diffs);
          this.diff_cleanupEfficiency(diffs);
        }
      } else if (a && typeof a == "object" && typeof opt_b == "undefined" && typeof opt_c == "undefined") {
        diffs = /** @type {!Array.<!diff_match_patch.Diff>} */
        a;
        text1 = this.diff_text1(diffs);
      } else if (typeof a == "string" && opt_b && typeof opt_b == "object" && typeof opt_c == "undefined") {
        text1 = /** @type {string} */
        a;
        diffs = /** @type {!Array.<!diff_match_patch.Diff>} */
        opt_b;
      } else if (typeof a == "string" && typeof opt_b == "string" && opt_c && typeof opt_c == "object") {
        text1 = /** @type {string} */
        a;
        diffs = /** @type {!Array.<!diff_match_patch.Diff>} */
        opt_c;
      } else {
        throw new Error("Unknown call format to patch_make.");
      }
      if (diffs.length === 0) {
        return [];
      }
      var patches = [];
      var patch = new diff_match_patch.patch_obj();
      var patchDiffLength = 0;
      var char_count1 = 0;
      var char_count2 = 0;
      var prepatch_text = text1;
      var postpatch_text = text1;
      for (var x = 0; x < diffs.length; x++) {
        var diff_type = diffs[x][0];
        var diff_text = diffs[x][1];
        if (!patchDiffLength && diff_type !== DIFF_EQUAL) {
          patch.start1 = char_count1;
          patch.start2 = char_count2;
        }
        switch (diff_type) {
          case DIFF_INSERT:
            patch.diffs[patchDiffLength++] = diffs[x];
            patch.length2 += diff_text.length;
            postpatch_text = postpatch_text.substring(0, char_count2) + diff_text + postpatch_text.substring(char_count2);
            break;
          case DIFF_DELETE:
            patch.length1 += diff_text.length;
            patch.diffs[patchDiffLength++] = diffs[x];
            postpatch_text = postpatch_text.substring(0, char_count2) + postpatch_text.substring(char_count2 + diff_text.length);
            break;
          case DIFF_EQUAL:
            if (diff_text.length <= 2 * this.Patch_Margin && patchDiffLength && diffs.length != x + 1) {
              patch.diffs[patchDiffLength++] = diffs[x];
              patch.length1 += diff_text.length;
              patch.length2 += diff_text.length;
            } else if (diff_text.length >= 2 * this.Patch_Margin) {
              if (patchDiffLength) {
                this.patch_addContext_(patch, prepatch_text);
                patches.push(patch);
                patch = new diff_match_patch.patch_obj();
                patchDiffLength = 0;
                prepatch_text = postpatch_text;
                char_count1 = char_count2;
              }
            }
            break;
        }
        if (diff_type !== DIFF_INSERT) {
          char_count1 += diff_text.length;
        }
        if (diff_type !== DIFF_DELETE) {
          char_count2 += diff_text.length;
        }
      }
      if (patchDiffLength) {
        this.patch_addContext_(patch, prepatch_text);
        patches.push(patch);
      }
      return patches;
    };
    diff_match_patch.prototype.patch_deepCopy = function(patches) {
      var patchesCopy = [];
      for (var x = 0; x < patches.length; x++) {
        var patch = patches[x];
        var patchCopy = new diff_match_patch.patch_obj();
        patchCopy.diffs = [];
        for (var y = 0; y < patch.diffs.length; y++) {
          patchCopy.diffs[y] = new diff_match_patch.Diff(patch.diffs[y][0], patch.diffs[y][1]);
        }
        patchCopy.start1 = patch.start1;
        patchCopy.start2 = patch.start2;
        patchCopy.length1 = patch.length1;
        patchCopy.length2 = patch.length2;
        patchesCopy[x] = patchCopy;
      }
      return patchesCopy;
    };
    diff_match_patch.prototype.patch_apply = function(patches, text) {
      if (patches.length == 0) {
        return [text, []];
      }
      patches = this.patch_deepCopy(patches);
      var nullPadding = this.patch_addPadding(patches);
      text = nullPadding + text + nullPadding;
      this.patch_splitMax(patches);
      var delta = 0;
      var results = [];
      for (var x = 0; x < patches.length; x++) {
        var expected_loc = patches[x].start2 + delta;
        var text1 = this.diff_text1(patches[x].diffs);
        var start_loc;
        var end_loc = -1;
        if (text1.length > this.Match_MaxBits) {
          start_loc = this.match_main(
            text,
            text1.substring(0, this.Match_MaxBits),
            expected_loc
          );
          if (start_loc != -1) {
            end_loc = this.match_main(
              text,
              text1.substring(text1.length - this.Match_MaxBits),
              expected_loc + text1.length - this.Match_MaxBits
            );
            if (end_loc == -1 || start_loc >= end_loc) {
              start_loc = -1;
            }
          }
        } else {
          start_loc = this.match_main(text, text1, expected_loc);
        }
        if (start_loc == -1) {
          results[x] = false;
          delta -= patches[x].length2 - patches[x].length1;
        } else {
          results[x] = true;
          delta = start_loc - expected_loc;
          var text2;
          if (end_loc == -1) {
            text2 = text.substring(start_loc, start_loc + text1.length);
          } else {
            text2 = text.substring(start_loc, end_loc + this.Match_MaxBits);
          }
          if (text1 == text2) {
            text = text.substring(0, start_loc) + this.diff_text2(patches[x].diffs) + text.substring(start_loc + text1.length);
          } else {
            var diffs = this.diff_main(text1, text2, false);
            if (text1.length > this.Match_MaxBits && this.diff_levenshtein(diffs) / text1.length > this.Patch_DeleteThreshold) {
              results[x] = false;
            } else {
              this.diff_cleanupSemanticLossless(diffs);
              var index1 = 0;
              var index2;
              for (var y = 0; y < patches[x].diffs.length; y++) {
                var mod = patches[x].diffs[y];
                if (mod[0] !== DIFF_EQUAL) {
                  index2 = this.diff_xIndex(diffs, index1);
                }
                if (mod[0] === DIFF_INSERT) {
                  text = text.substring(0, start_loc + index2) + mod[1] + text.substring(start_loc + index2);
                } else if (mod[0] === DIFF_DELETE) {
                  text = text.substring(0, start_loc + index2) + text.substring(start_loc + this.diff_xIndex(
                    diffs,
                    index1 + mod[1].length
                  ));
                }
                if (mod[0] !== DIFF_DELETE) {
                  index1 += mod[1].length;
                }
              }
            }
          }
        }
      }
      text = text.substring(nullPadding.length, text.length - nullPadding.length);
      return [text, results];
    };
    diff_match_patch.prototype.patch_addPadding = function(patches) {
      var paddingLength = this.Patch_Margin;
      var nullPadding = "";
      for (var x = 1; x <= paddingLength; x++) {
        nullPadding += String.fromCharCode(x);
      }
      for (var x = 0; x < patches.length; x++) {
        patches[x].start1 += paddingLength;
        patches[x].start2 += paddingLength;
      }
      var patch = patches[0];
      var diffs = patch.diffs;
      if (diffs.length == 0 || diffs[0][0] != DIFF_EQUAL) {
        diffs.unshift(new diff_match_patch.Diff(DIFF_EQUAL, nullPadding));
        patch.start1 -= paddingLength;
        patch.start2 -= paddingLength;
        patch.length1 += paddingLength;
        patch.length2 += paddingLength;
      } else if (paddingLength > diffs[0][1].length) {
        var extraLength = paddingLength - diffs[0][1].length;
        diffs[0][1] = nullPadding.substring(diffs[0][1].length) + diffs[0][1];
        patch.start1 -= extraLength;
        patch.start2 -= extraLength;
        patch.length1 += extraLength;
        patch.length2 += extraLength;
      }
      patch = patches[patches.length - 1];
      diffs = patch.diffs;
      if (diffs.length == 0 || diffs[diffs.length - 1][0] != DIFF_EQUAL) {
        diffs.push(new diff_match_patch.Diff(DIFF_EQUAL, nullPadding));
        patch.length1 += paddingLength;
        patch.length2 += paddingLength;
      } else if (paddingLength > diffs[diffs.length - 1][1].length) {
        var extraLength = paddingLength - diffs[diffs.length - 1][1].length;
        diffs[diffs.length - 1][1] += nullPadding.substring(0, extraLength);
        patch.length1 += extraLength;
        patch.length2 += extraLength;
      }
      return nullPadding;
    };
    diff_match_patch.prototype.patch_splitMax = function(patches) {
      var patch_size = this.Match_MaxBits;
      for (var x = 0; x < patches.length; x++) {
        if (patches[x].length1 <= patch_size) {
          continue;
        }
        var bigpatch = patches[x];
        patches.splice(x--, 1);
        var start1 = bigpatch.start1;
        var start2 = bigpatch.start2;
        var precontext = "";
        while (bigpatch.diffs.length !== 0) {
          var patch = new diff_match_patch.patch_obj();
          var empty = true;
          patch.start1 = start1 - precontext.length;
          patch.start2 = start2 - precontext.length;
          if (precontext !== "") {
            patch.length1 = patch.length2 = precontext.length;
            patch.diffs.push(new diff_match_patch.Diff(DIFF_EQUAL, precontext));
          }
          while (bigpatch.diffs.length !== 0 && patch.length1 < patch_size - this.Patch_Margin) {
            var diff_type = bigpatch.diffs[0][0];
            var diff_text = bigpatch.diffs[0][1];
            if (diff_type === DIFF_INSERT) {
              patch.length2 += diff_text.length;
              start2 += diff_text.length;
              patch.diffs.push(bigpatch.diffs.shift());
              empty = false;
            } else if (diff_type === DIFF_DELETE && patch.diffs.length == 1 && patch.diffs[0][0] == DIFF_EQUAL && diff_text.length > 2 * patch_size) {
              patch.length1 += diff_text.length;
              start1 += diff_text.length;
              empty = false;
              patch.diffs.push(new diff_match_patch.Diff(diff_type, diff_text));
              bigpatch.diffs.shift();
            } else {
              diff_text = diff_text.substring(
                0,
                patch_size - patch.length1 - this.Patch_Margin
              );
              patch.length1 += diff_text.length;
              start1 += diff_text.length;
              if (diff_type === DIFF_EQUAL) {
                patch.length2 += diff_text.length;
                start2 += diff_text.length;
              } else {
                empty = false;
              }
              patch.diffs.push(new diff_match_patch.Diff(diff_type, diff_text));
              if (diff_text == bigpatch.diffs[0][1]) {
                bigpatch.diffs.shift();
              } else {
                bigpatch.diffs[0][1] = bigpatch.diffs[0][1].substring(diff_text.length);
              }
            }
          }
          precontext = this.diff_text2(patch.diffs);
          precontext = precontext.substring(precontext.length - this.Patch_Margin);
          var postcontext = this.diff_text1(bigpatch.diffs).substring(0, this.Patch_Margin);
          if (postcontext !== "") {
            patch.length1 += postcontext.length;
            patch.length2 += postcontext.length;
            if (patch.diffs.length !== 0 && patch.diffs[patch.diffs.length - 1][0] === DIFF_EQUAL) {
              patch.diffs[patch.diffs.length - 1][1] += postcontext;
            } else {
              patch.diffs.push(new diff_match_patch.Diff(DIFF_EQUAL, postcontext));
            }
          }
          if (!empty) {
            patches.splice(++x, 0, patch);
          }
        }
      }
    };
    diff_match_patch.prototype.patch_toText = function(patches) {
      var text = [];
      for (var x = 0; x < patches.length; x++) {
        text[x] = patches[x];
      }
      return text.join("");
    };
    diff_match_patch.prototype.patch_fromText = function(textline) {
      var patches = [];
      if (!textline) {
        return patches;
      }
      var text = textline.split("\n");
      var textPointer = 0;
      var patchHeader = /^@@ -(\d+),?(\d*) \+(\d+),?(\d*) @@$/;
      while (textPointer < text.length) {
        var m = text[textPointer].match(patchHeader);
        if (!m) {
          throw new Error("Invalid patch string: " + text[textPointer]);
        }
        var patch = new diff_match_patch.patch_obj();
        patches.push(patch);
        patch.start1 = parseInt(m[1], 10);
        if (m[2] === "") {
          patch.start1--;
          patch.length1 = 1;
        } else if (m[2] == "0") {
          patch.length1 = 0;
        } else {
          patch.start1--;
          patch.length1 = parseInt(m[2], 10);
        }
        patch.start2 = parseInt(m[3], 10);
        if (m[4] === "") {
          patch.start2--;
          patch.length2 = 1;
        } else if (m[4] == "0") {
          patch.length2 = 0;
        } else {
          patch.start2--;
          patch.length2 = parseInt(m[4], 10);
        }
        textPointer++;
        while (textPointer < text.length) {
          var sign = text[textPointer].charAt(0);
          try {
            var line = decodeURI(text[textPointer].substring(1));
          } catch (ex) {
            throw new Error("Illegal escape in patch_fromText: " + line);
          }
          if (sign == "-") {
            patch.diffs.push(new diff_match_patch.Diff(DIFF_DELETE, line));
          } else if (sign == "+") {
            patch.diffs.push(new diff_match_patch.Diff(DIFF_INSERT, line));
          } else if (sign == " ") {
            patch.diffs.push(new diff_match_patch.Diff(DIFF_EQUAL, line));
          } else if (sign == "@") {
            break;
          } else if (sign === "") {
          } else {
            throw new Error('Invalid patch mode "' + sign + '" in: ' + line);
          }
          textPointer++;
        }
      }
      return patches;
    };
    diff_match_patch.patch_obj = function() {
      this.diffs = [];
      this.start1 = null;
      this.start2 = null;
      this.length1 = 0;
      this.length2 = 0;
    };
    diff_match_patch.patch_obj.prototype.toString = function() {
      var coords1, coords2;
      if (this.length1 === 0) {
        coords1 = this.start1 + ",0";
      } else if (this.length1 == 1) {
        coords1 = this.start1 + 1;
      } else {
        coords1 = this.start1 + 1 + "," + this.length1;
      }
      if (this.length2 === 0) {
        coords2 = this.start2 + ",0";
      } else if (this.length2 == 1) {
        coords2 = this.start2 + 1;
      } else {
        coords2 = this.start2 + 1 + "," + this.length2;
      }
      var text = ["@@ -" + coords1 + " +" + coords2 + " @@\n"];
      var op;
      for (var x = 0; x < this.diffs.length; x++) {
        switch (this.diffs[x][0]) {
          case DIFF_INSERT:
            op = "+";
            break;
          case DIFF_DELETE:
            op = "-";
            break;
          case DIFF_EQUAL:
            op = " ";
            break;
        }
        text[x + 1] = op + encodeURI(this.diffs[x][1]) + "\n";
      }
      return text.join("").replace(/%20/g, " ");
    };
    module2.exports = diff_match_patch;
    module2.exports["diff_match_patch"] = diff_match_patch;
    module2.exports["DIFF_DELETE"] = DIFF_DELETE;
    module2.exports["DIFF_INSERT"] = DIFF_INSERT;
    module2.exports["DIFF_EQUAL"] = DIFF_EQUAL;
  }
});

// ../shared/dist/merge.js
var require_merge = __commonJS({
  "../shared/dist/merge.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.threeWayMerge = threeWayMerge2;
    exports.generateConflictPath = generateConflictPath2;
    exports.formatConflictMarkers = formatConflictMarkers;
    var diff_match_patch_1 = require_diff_match_patch();
    var dmp = new diff_match_patch_1.diff_match_patch();
    function threeWayMerge2(baseText, localText, remoteText, filePath) {
      if (localText === remoteText) {
        return { hasConflict: false, mergedText: localText };
      }
      if (localText === baseText) {
        return { hasConflict: false, mergedText: remoteText };
      }
      if (remoteText === baseText) {
        return { hasConflict: false, mergedText: localText };
      }
      if (!baseText) {
        const conflictFileName2 = generateConflictPath2(filePath);
        return {
          hasConflict: true,
          mergedText: localText,
          conflictDetails: {
            baseText: "",
            localText,
            remoteText,
            conflictingPath: conflictFileName2
          }
        };
      }
      try {
        const patches = dmp.patch_make(baseText, remoteText);
        const [patchedText, results] = dmp.patch_apply(patches, localText);
        const allSucceeded = results.every(Boolean);
        if (allSucceeded) {
          return {
            hasConflict: false,
            mergedText: patchedText
          };
        }
      } catch {
      }
      const conflictFileName = generateConflictPath2(filePath);
      return {
        hasConflict: true,
        mergedText: localText,
        conflictDetails: {
          baseText,
          localText,
          remoteText,
          conflictingPath: conflictFileName
        }
      };
    }
    function generateConflictPath2(originalPath, timestamp = Date.now()) {
      const d = new Date(timestamp);
      const pad = (n) => n.toString().padStart(2, "0");
      const dateStr = `${d.getFullYear()}${pad(d.getMonth() + 1)}${pad(d.getDate())}-${pad(d.getHours())}${pad(d.getMinutes())}${pad(d.getSeconds())}`;
      const lastDot = originalPath.lastIndexOf(".");
      if (lastDot === -1 || lastDot === 0) {
        return `${originalPath}.sync-conflict-${dateStr}`;
      }
      const base = originalPath.substring(0, lastDot);
      const ext = originalPath.substring(lastDot);
      return `${base}.sync-conflict-${dateStr}${ext}`;
    }
    function formatConflictMarkers(localText, remoteText, deviceName = "Remote Device") {
      return `<<<<<<< Local Changes
${localText}
=======
${remoteText}
>>>>>>> Changes from ${deviceName}
`;
    }
  }
});

// ../shared/dist/ignore.js
var require_ignore = __commonJS({
  "../shared/dist/ignore.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.IgnoreFilter = void 0;
    var types_1 = require_types();
    function globToRegex(pattern) {
      let p = pattern.trim();
      if (!p || p.startsWith("#")) {
        return /^$/;
      }
      p = p.replace(/\\/g, "/");
      const matchFromRoot = p.startsWith("/");
      if (matchFromRoot) {
        p = p.substring(1);
      }
      const isDirectoryOnly = p.endsWith("/");
      if (isDirectoryOnly) {
        p = p.substring(0, p.length - 1);
      }
      if (p.startsWith("**/")) {
        p = p.substring(3);
      }
      let regexStr = p.replace(/[.+^${}()|[\]\\]/g, "\\$&");
      regexStr = regexStr.replace(/\*\*/g, "___GLOBSTAR___").replace(/\*/g, "[^/]*").replace(/___GLOBSTAR___/g, ".*").replace(/\?/g, "[^/]");
      if (matchFromRoot) {
        regexStr = `^${regexStr}`;
      } else {
        regexStr = `(^|.*/)${regexStr}`;
      }
      if (isDirectoryOnly) {
        regexStr = `${regexStr}(/.*)?$`;
      } else {
        regexStr = `${regexStr}(/.*)?$`;
      }
      return new RegExp(regexStr);
    }
    var IgnoreFilter3 = class {
      constructor(options = {}, customIgnoreContent = "") {
        __publicField(this, "patterns", []);
        __publicField(this, "options");
        this.options = { ...types_1.DEFAULT_SYNC_OPTIONS, ...options };
        this.reloadPatterns(customIgnoreContent);
      }
      reloadPatterns(customIgnoreContent = "") {
        const rawPatterns = [...this.options.ignoredPatterns];
        if (customIgnoreContent) {
          const lines = customIgnoreContent.split(/\r?\n/);
          for (const line of lines) {
            const trimmed = line.trim();
            if (trimmed && !trimmed.startsWith("#")) {
              rawPatterns.push(trimmed);
            }
          }
        }
        if (!this.options.syncWorkspace) {
          rawPatterns.push(".obsidian/workspace.json");
          rawPatterns.push(".obsidian/workspace-mobile.json");
          rawPatterns.push(".obsidian/workspaces.json");
        }
        if (!this.options.syncObsidianConfig) {
          rawPatterns.push(".obsidian/**");
        }
        this.patterns = rawPatterns.map((p) => globToRegex(p)).filter((r) => r.source !== "^$");
      }
      isIgnored(rawPath) {
        const normalized = rawPath.replace(/\\/g, "/").replace(/^\/+/, "");
        if (!normalized)
          return true;
        for (const regex of this.patterns) {
          if (regex.test(normalized)) {
            return true;
          }
        }
        return false;
      }
    };
    exports.IgnoreFilter = IgnoreFilter3;
  }
});

// ../shared/dist/index.js
var require_dist = __commonJS({
  "../shared/dist/index.js"(exports) {
    "use strict";
    var __createBinding = exports && exports.__createBinding || (Object.create ? function(o, m, k, k2) {
      if (k2 === void 0)
        k2 = k;
      var desc = Object.getOwnPropertyDescriptor(m, k);
      if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
        desc = { enumerable: true, get: function() {
          return m[k];
        } };
      }
      Object.defineProperty(o, k2, desc);
    } : function(o, m, k, k2) {
      if (k2 === void 0)
        k2 = k;
      o[k2] = m[k];
    });
    var __exportStar = exports && exports.__exportStar || function(m, exports2) {
      for (var p in m)
        if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports2, p))
          __createBinding(exports2, m, p);
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    __exportStar(require_types(), exports);
    __exportStar(require_protocol(), exports);
    __exportStar(require_hash(), exports);
    __exportStar(require_merge(), exports);
    __exportStar(require_ignore(), exports);
  }
});

// src/main.ts
var main_exports = {};
__export(main_exports, {
  default: () => VPSVaultSyncPlugin
});
module.exports = __toCommonJS(main_exports);
var import_obsidian6 = require("obsidian");

// src/settings.ts
var import_obsidian = require("obsidian");
var DEFAULT_SETTINGS = {
  serverUrl: "ws://localhost:3000",
  authToken: "",
  deviceName: "My Device",
  clientId: "",
  syncObsidianConfig: true,
  syncWorkspace: false,
  conflictStrategy: "three-way",
  autoSyncOnStart: true,
  liveSyncEnabled: true,
  debounceMs: 500,
  lastSyncedTimestamp: 0
};
var SyncSettingTab = class extends import_obsidian.PluginSettingTab {
  constructor(app, plugin) {
    super(app, plugin);
    this.plugin = plugin;
  }
  display() {
    const { containerEl } = this;
    containerEl.empty();
    containerEl.createEl("h2", { text: "VPS Live Vault Sync" });
    containerEl.createEl("p", {
      text: "Synchronize your notes, attachments, themes, and plugins seamlessly in real time with your VPS server.",
      cls: "setting-item-description"
    });
    new import_obsidian.Setting(containerEl).setName("Server URL").setDesc("WebSocket or HTTP address of your VPS sync server (e.g. wss://sync.yourdomain.com or ws://192.168.1.100:3000)").addText((text) => text.setPlaceholder("wss://sync.example.com").setValue(this.plugin.settings.serverUrl).onChange(async (value) => {
      this.plugin.settings.serverUrl = value.trim();
      await this.plugin.saveSettings();
    }));
    new import_obsidian.Setting(containerEl).setName("Secret API Token").setDesc("The secret authentication token configured on your VPS server (SYNC_TOKEN).").addText((text) => {
      text.inputEl.type = "password";
      text.setPlaceholder("Enter your secret token").setValue(this.plugin.settings.authToken).onChange(async (value) => {
        this.plugin.settings.authToken = value.trim();
        await this.plugin.saveSettings();
      });
    });
    new import_obsidian.Setting(containerEl).setName("Device Name").setDesc("Identifier for this device shown in sync logs and conflict markers.").addText((text) => text.setPlaceholder("e.g. MacBook Pro, iPhone, Windows Desktop").setValue(this.plugin.settings.deviceName).onChange(async (value) => {
      this.plugin.settings.deviceName = value.trim();
      await this.plugin.saveSettings();
    }));
    new import_obsidian.Setting(containerEl).setName("Connection Status").setDesc(`Current state: ${this.plugin.syncClient.getStatus()}`).addButton((btn) => btn.setButtonText("Test Connection").setCta().onClick(async () => {
      btn.setDisabled(true);
      btn.setButtonText("Testing...");
      try {
        const success = await this.plugin.syncClient.testConnection();
        if (success) {
          new import_obsidian.Notice("\u2705 Connection to VPS sync server successful!");
        } else {
          new import_obsidian.Notice("\u274C Connection failed. Check server URL and secret token.");
        }
      } catch (err) {
        new import_obsidian.Notice(`\u274C Connection error: ${err instanceof Error ? err.message : String(err)}`);
      } finally {
        btn.setDisabled(false);
        btn.setButtonText("Test Connection");
        this.display();
      }
    })).addButton((btn) => btn.setButtonText("Reconnect").onClick(() => {
      this.plugin.syncClient.reconnect();
      new import_obsidian.Notice("Reconnecting to VPS sync server...");
      this.display();
    }));
    containerEl.createEl("h3", { text: "Synchronization Preferences" });
    new import_obsidian.Setting(containerEl).setName("Live Real-Time Sync").setDesc("Continuously push and pull changes as they happen.").addToggle((toggle) => toggle.setValue(this.plugin.settings.liveSyncEnabled).onChange(async (value) => {
      this.plugin.settings.liveSyncEnabled = value;
      await this.plugin.saveSettings();
      if (value) {
        this.plugin.syncClient.connect();
      } else {
        this.plugin.syncClient.disconnect();
      }
    }));
    new import_obsidian.Setting(containerEl).setName("Sync .obsidian Configuration").setDesc("Sync community plugins, themes, snippets, and app settings.").addToggle((toggle) => toggle.setValue(this.plugin.settings.syncObsidianConfig).onChange(async (value) => {
      this.plugin.settings.syncObsidianConfig = value;
      await this.plugin.saveSettings();
    }));
    new import_obsidian.Setting(containerEl).setName("Sync Workspace Layout").setDesc("Sync open tabs and sidebars (workspace.json). Disable if you prefer different layouts on mobile vs desktop.").addToggle((toggle) => toggle.setValue(this.plugin.settings.syncWorkspace).onChange(async (value) => {
      this.plugin.settings.syncWorkspace = value;
      await this.plugin.saveSettings();
    }));
    new import_obsidian.Setting(containerEl).setName("Conflict Resolution Strategy").setDesc("How to handle simultaneous edits to the same file.").addDropdown((drop) => drop.addOption("three-way", "3-Way Line Merge (Auto-merge non-overlapping lines, create conflict file on collision)").addOption("last-write-wins", "Last-Write-Wins (Overwrite with latest timestamp)").setValue(this.plugin.settings.conflictStrategy).onChange(async (value) => {
      this.plugin.settings.conflictStrategy = value;
      await this.plugin.saveSettings();
    }));
    new import_obsidian.Setting(containerEl).setName("Debounce Delay (ms)").setDesc("Delay after typing before broadcasting changes to prevent server spam.").addSlider((slider) => slider.setLimits(200, 2e3, 100).setValue(this.plugin.settings.debounceMs).setDynamicTooltip().onChange(async (value) => {
      this.plugin.settings.debounceMs = value;
      await this.plugin.saveSettings();
    }));
    containerEl.createEl("h3", { text: "Manual Operations & Diagnostics" });
    new import_obsidian.Setting(containerEl).setName("Force Full Reconciliation").setDesc("Compare all files with server and synchronize differences immediately.").addButton((btn) => btn.setButtonText("Sync Now").onClick(async () => {
      new import_obsidian.Notice("Starting full vault reconciliation...");
      await this.plugin.syncClient.reconcileVault();
    }));
    new import_obsidian.Setting(containerEl).setName("View Sync Activity Log").setDesc("Review recent file transfers, connection events, and conflict records.").addButton((btn) => btn.setButtonText("Open Activity Log").onClick(() => {
      this.plugin.openActivityLogModal();
    }));
  }
};

// src/sync-client.ts
var import_obsidian2 = require("obsidian");
var import_shared = __toESM(require_dist());
var SyncClient = class {
  constructor(app, plugin) {
    this.ws = null;
    this.status = "disconnected";
    this.reconnectTimeout = null;
    this.reconnectAttempts = 0;
    this.pendingRequests = /* @__PURE__ */ new Map();
    this.statusListeners = [];
    this.activityLogs = [];
    this.isReconciling = false;
    this.app = app;
    this.plugin = plugin;
  }
  getStatus() {
    return this.status;
  }
  getLogs() {
    return [...this.activityLogs];
  }
  logActivity(type, path, message) {
    const entry = {
      id: `${Date.now()}-${Math.random().toString(36).substring(2, 6)}`,
      timestamp: Date.now(),
      type,
      path,
      message
    };
    this.activityLogs.unshift(entry);
    if (this.activityLogs.length > 200) {
      this.activityLogs.pop();
    }
  }
  onStatusChange(listener) {
    this.statusListeners.push(listener);
  }
  setStatus(newStatus, detail) {
    this.status = newStatus;
    for (const listener of this.statusListeners) {
      try {
        listener(newStatus, detail);
      } catch (err) {
        console.error("[SyncClient] Error in status listener:", err);
      }
    }
  }
  connect() {
    if (!this.plugin.settings.liveSyncEnabled || !this.plugin.settings.serverUrl || !this.plugin.settings.authToken) {
      this.setStatus("disconnected", "Server URL or token not configured");
      return;
    }
    if (this.ws && (this.ws.readyState === WebSocket.OPEN || this.ws.readyState === WebSocket.CONNECTING)) {
      return;
    }
    this.clearReconnectTimeout();
    this.setStatus("connecting");
    try {
      let wsUrl = this.plugin.settings.serverUrl.trim();
      if (wsUrl.startsWith("http://")) {
        wsUrl = wsUrl.replace("http://", "ws://");
      } else if (wsUrl.startsWith("https://")) {
        wsUrl = wsUrl.replace("https://", "wss://");
      }
      this.ws = new WebSocket(wsUrl);
      this.ws.onopen = () => {
        this.reconnectAttempts = 0;
        this.setStatus("authenticating");
        this.sendHandshake();
      };
      this.ws.onmessage = async (event) => {
        try {
          const rawData = typeof event.data === "string" ? event.data : await event.data.text();
          const message = JSON.parse(rawData);
          await this.handleMessage(message);
        } catch (err) {
          console.error("[SyncClient] Failed to parse incoming WebSocket message:", err);
        }
      };
      this.ws.onclose = (event) => {
        this.cleanupWs();
        if (event.code === 4001) {
          this.setStatus("error", "Authentication failed (check token)");
          this.logActivity("error", "", "Authentication failed: Invalid secret token");
          new import_obsidian2.Notice("\u274C VPS Sync: Authentication failed. Please verify your secret token in settings.");
        } else {
          this.setStatus("disconnected", "Connection closed");
          this.scheduleReconnect();
        }
      };
      this.ws.onerror = (err) => {
        console.error("[SyncClient] WebSocket error:", err);
        this.setStatus("error", "Connection error");
      };
    } catch (err) {
      console.error("[SyncClient] Connect error:", err);
      this.setStatus("error", err instanceof Error ? err.message : "Connect failed");
      this.scheduleReconnect();
    }
  }
  disconnect() {
    this.clearReconnectTimeout();
    if (this.ws) {
      this.ws.close(1e3, "User disconnected");
      this.cleanupWs();
    }
    this.setStatus("disconnected");
  }
  reconnect() {
    this.disconnect();
    this.connect();
  }
  async testConnection() {
    return new Promise((resolve) => {
      let testWs = null;
      let timeout = null;
      try {
        let wsUrl = this.plugin.settings.serverUrl.trim();
        if (wsUrl.startsWith("http://"))
          wsUrl = wsUrl.replace("http://", "ws://");
        else if (wsUrl.startsWith("https://"))
          wsUrl = wsUrl.replace("https://", "wss://");
        testWs = new WebSocket(wsUrl);
        timeout = window.setTimeout(() => {
          if (testWs)
            testWs.close();
          resolve(false);
        }, 6e3);
        testWs.onopen = () => {
          const authMsg = {
            id: `test-${Date.now()}`,
            type: "AUTH_REQUEST",
            timestamp: Date.now(),
            token: this.plugin.settings.authToken,
            clientId: this.plugin.settings.clientId,
            clientName: this.plugin.settings.deviceName,
            protocolVersion: import_shared.PROTOCOL_VERSION,
            deviceType: "desktop"
          };
          testWs?.send(JSON.stringify(authMsg));
        };
        testWs.onmessage = (event) => {
          try {
            const msg = JSON.parse(event.data);
            if (msg.type === "AUTH_RESPONSE") {
              const resp = msg;
              if (timeout)
                window.clearTimeout(timeout);
              testWs?.close();
              resolve(resp.success);
            }
          } catch {
            resolve(false);
          }
        };
        testWs.onerror = () => {
          if (timeout)
            window.clearTimeout(timeout);
          resolve(false);
        };
      } catch {
        if (timeout)
          window.clearTimeout(timeout);
        resolve(false);
      }
    });
  }
  sendHandshake() {
    const authMsg = {
      id: `auth-${Date.now()}`,
      type: "AUTH_REQUEST",
      timestamp: Date.now(),
      token: this.plugin.settings.authToken,
      clientId: this.plugin.settings.clientId,
      clientName: this.plugin.settings.deviceName,
      protocolVersion: import_shared.PROTOCOL_VERSION,
      deviceType: "desktop"
    };
    this.send(authMsg);
  }
  scheduleReconnect() {
    if (!this.plugin.settings.liveSyncEnabled)
      return;
    this.clearReconnectTimeout();
    this.reconnectAttempts++;
    const delay = Math.min(3e4, 1e3 * Math.pow(1.5, this.reconnectAttempts) + Math.random() * 1e3);
    this.reconnectTimeout = window.setTimeout(() => {
      if (this.status !== "synced" && this.status !== "syncing") {
        this.connect();
      }
    }, delay);
  }
  clearReconnectTimeout() {
    if (this.reconnectTimeout !== null) {
      window.clearTimeout(this.reconnectTimeout);
      this.reconnectTimeout = null;
    }
  }
  cleanupWs() {
    if (this.ws) {
      this.ws.onopen = null;
      this.ws.onmessage = null;
      this.ws.onclose = null;
      this.ws.onerror = null;
      this.ws = null;
    }
    for (const req of this.pendingRequests.values()) {
      window.clearTimeout(req.timer);
      req.reject(new Error("WebSocket connection closed"));
    }
    this.pendingRequests.clear();
  }
  send(message) {
    if (this.ws && this.ws.readyState === WebSocket.OPEN) {
      this.ws.send(JSON.stringify(message));
    }
  }
  async request(message, timeoutMs = 15e3) {
    return new Promise((resolve, reject) => {
      if (!this.ws || this.ws.readyState !== WebSocket.OPEN) {
        return reject(new Error("WebSocket not connected"));
      }
      const timer = window.setTimeout(() => {
        this.pendingRequests.delete(message.id);
        reject(new Error(`Request timeout for message type: ${message.type}`));
      }, timeoutMs);
      this.pendingRequests.set(message.id, { resolve, reject, timer });
      this.send(message);
    });
  }
  async handleMessage(message) {
    if (this.pendingRequests.has(message.id)) {
      const { resolve, timer } = this.pendingRequests.get(message.id);
      window.clearTimeout(timer);
      this.pendingRequests.delete(message.id);
      resolve(message);
    }
    switch (message.type) {
      case "AUTH_RESPONSE": {
        const resp = message;
        if (resp.success) {
          this.logActivity("info", "", `Connected and authenticated with ${resp.vaultName}`);
          this.setStatus("reconciling");
          await this.reconcileVault();
        } else {
          this.setStatus("error", resp.error || "Auth failed");
          this.logActivity("error", "", `Authentication error: ${resp.error}`);
        }
        break;
      }
      case "SYNC_EVENT": {
        const eventMsg = message;
        await this.handleRemoteSyncEvent(eventMsg.event);
        break;
      }
      case "ERROR_NOTIFICATION": {
        console.error("[SyncClient] Server error notification:", message);
        this.logActivity("error", "", `Server Error: ${message.message}`);
        break;
      }
    }
  }
  /**
   * Applies incoming sync event from server (originating from VPS filesystem or another device)
   */
  async handleRemoteSyncEvent(event) {
    this.setStatus("syncing");
    try {
      if (event.type === "create" || event.type === "modify") {
        await this.applyRemoteFileWrite(event);
      } else if (event.type === "delete") {
        await this.applyRemoteFileDelete(event);
      } else if (event.type === "rename") {
        await this.applyRemoteFileRename(event);
      }
      this.setStatus("synced");
    } catch (err) {
      console.error(`[SyncClient] Error applying remote sync event:`, err);
      this.logActivity("error", "path" in event ? event.path : "", `Failed to apply update: ${err instanceof Error ? err.message : String(err)}`);
      this.setStatus("synced");
    }
  }
  async applyRemoteFileWrite(event) {
    let content = event.content;
    if (content === void 0) {
      const getResp = await this.request({
        id: `get-${Date.now()}-${Math.random().toString(36).substring(2, 6)}`,
        type: "FILE_GET_REQUEST",
        timestamp: Date.now(),
        path: event.path
      });
      content = getResp.content || "";
    }
    const adapter = this.app.vault.adapter;
    const isBin = event.isBinary;
    const normalizedPath = event.path.replace(/\\/g, "/");
    this.plugin.vaultWatcher.suppressPath(normalizedPath, 4e3);
    const exists = await adapter.exists(normalizedPath);
    if (!isBin) {
      let incomingText = content;
      if (exists) {
        const currentLocalText = await adapter.read(normalizedPath);
        if (this.plugin.settings.conflictStrategy === "three-way") {
          const res = await this.plugin.conflictHandler.resolveTextConflict(
            normalizedPath,
            currentLocalText,
            incomingText
          );
          if (res.conflictOccurred) {
            this.logActivity("conflict", normalizedPath, `Conflict detected! Created ${res.conflictFileCreated}`);
            return;
          }
          incomingText = res.textToWrite;
        }
      }
      await this.ensureParentFolder(normalizedPath);
      await adapter.write(normalizedPath, incomingText);
      this.plugin.conflictHandler.recordBaseSnapshot(normalizedPath, incomingText);
      this.plugin.vaultWatcher.recordConfigHash(normalizedPath, event.hash);
      this.logActivity("download", normalizedPath, `Downloaded note (${event.type})`);
    } else {
      const buf = Buffer.from(content, "base64");
      const arrayBuf = buf.buffer.slice(buf.byteOffset, buf.byteOffset + buf.byteLength);
      await this.ensureParentFolder(normalizedPath);
      await adapter.writeBinary(normalizedPath, arrayBuf);
      this.plugin.vaultWatcher.recordConfigHash(normalizedPath, event.hash);
      this.logActivity("download", normalizedPath, `Downloaded binary asset`);
    }
    const configPrefix = (this.app.vault.configDir || ".obsidian") + "/";
    if (normalizedPath.startsWith(".obsidian/") || normalizedPath.startsWith(configPrefix)) {
      try {
        if (normalizedPath.includes("appearance.json") || normalizedPath.includes("/themes/") || normalizedPath.includes("/snippets/")) {
          const customCss = this.app.customCss;
          if (customCss) {
            if (typeof customCss.requestLoadTheme === "function")
              customCss.requestLoadTheme();
            if (typeof customCss.requestLoadSnippets === "function")
              customCss.requestLoadSnippets();
            if (typeof customCss.loadTheme === "function")
              customCss.loadTheme();
          }
        }
        if (normalizedPath.includes("/plugins/") || normalizedPath.includes("community-plugins.json")) {
          const plugins = this.app.plugins;
          if (plugins && typeof plugins.loadManifests === "function") {
            plugins.loadManifests();
          }
        }
      } catch {
      }
    }
  }
  async applyRemoteFileDelete(event) {
    const adapter = this.app.vault.adapter;
    const normalizedPath = event.path.replace(/\\/g, "/");
    this.plugin.vaultWatcher.suppressPath(normalizedPath, 4e3);
    this.plugin.conflictHandler.removeBaseSnapshot(normalizedPath);
    if (await adapter.exists(normalizedPath)) {
      await adapter.trashLocal(normalizedPath);
      this.logActivity("delete", normalizedPath, "Deleted file (moved to local trash)");
    }
  }
  async applyRemoteFileRename(event) {
    const adapter = this.app.vault.adapter;
    const oldNorm = event.oldPath.replace(/\\/g, "/");
    const newNorm = event.newPath.replace(/\\/g, "/");
    this.plugin.vaultWatcher.suppressPath(oldNorm, 4e3);
    this.plugin.vaultWatcher.suppressPath(newNorm, 4e3);
    if (await adapter.exists(oldNorm)) {
      await this.ensureParentFolder(newNorm);
      await adapter.rename(oldNorm, newNorm);
      this.logActivity("download", newNorm, `Renamed from ${oldNorm}`);
    }
  }
  async ensureParentFolder(filePath) {
    const parts = filePath.split("/");
    if (parts.length <= 1)
      return;
    parts.pop();
    const folderPath = parts.join("/");
    const adapter = this.app.vault.adapter;
    if (!await adapter.exists(folderPath)) {
      await adapter.mkdir(folderPath);
    }
  }
  /**
   * Called by VaultWatcher when a local file change happens in Obsidian
   */
  async onLocalSyncEvent(event, inlineContent) {
    if (this.status !== "synced" && this.status !== "syncing" && this.status !== "reconciling") {
      await this.plugin.offlineQueue.enqueue(event);
      this.logActivity("upload", "path" in event ? event.path : "", "Queued for sync (offline)");
      return;
    }
    this.setStatus("syncing");
    try {
      if (event.type === "create" || event.type === "modify") {
        const contentToSend = inlineContent !== void 0 ? inlineContent : event.content || "";
        const putMsg = {
          id: `put-${Date.now()}-${Math.random().toString(36).substring(2, 6)}`,
          type: "FILE_PUT_REQUEST",
          timestamp: Date.now(),
          path: event.path,
          content: contentToSend,
          isBinary: event.isBinary,
          mtime: event.mtime,
          hash: event.hash,
          baseHash: event.baseHash
        };
        const resp = await this.request(putMsg);
        if (resp.conflictOccurred) {
          this.logActivity("conflict", event.path, `Server collision: conflict saved as ${resp.conflictPath}`);
          new import_obsidian2.Notice(`\u26A0\uFE0F Server Conflict on ${event.path}. Backup saved as ${resp.conflictPath}`);
        } else {
          this.logActivity("upload", event.path, `Uploaded ${event.type}`);
        }
      } else {
        const syncMsg = {
          id: `evt-${Date.now()}-${Math.random().toString(36).substring(2, 6)}`,
          type: "SYNC_EVENT",
          timestamp: Date.now(),
          event
        };
        const ack = await this.request(syncMsg);
        if (ack.success) {
          this.logActivity("upload", "path" in event ? event.path : "", `Synced ${event.type}`);
        }
      }
    } catch (err) {
      console.error("[SyncClient] Error sending local sync event:", err);
      await this.plugin.offlineQueue.enqueue(event);
    } finally {
      this.setStatus("synced");
    }
  }
  /**
   * Performs complete vault reconciliation with VPS server manifest
   */
  async reconcileVault() {
    if (this.isReconciling)
      return;
    this.isReconciling = true;
    this.setStatus("reconciling");
    try {
      this.logActivity("info", "", "Building local vault manifest...");
      const localManifest = await this.buildLocalManifest();
      this.logActivity("info", "", "Requesting manifest diff from server...");
      const diffResp = await this.request({
        id: `diff-${Date.now()}`,
        type: "MANIFEST_DIFF_REQUEST",
        timestamp: Date.now(),
        clientManifest: localManifest
      });
      const { diff, serverManifest } = diffResp;
      this.logActivity("info", "", `Diff summary: ${diff.toDownload.length} to download, ${diff.toUpload.length} to upload`);
      for (const p of diff.toDownload) {
        const serverMeta = serverManifest[p];
        if (!serverMeta)
          continue;
        try {
          const getResp = await this.request({
            id: `get-${Date.now()}-${Math.random().toString(36).substring(2, 6)}`,
            type: "FILE_GET_REQUEST",
            timestamp: Date.now(),
            path: p
          });
          if (getResp.exists && getResp.content !== void 0) {
            await this.applyRemoteFileWrite({
              id: `rec-${Date.now()}`,
              clientId: "server",
              timestamp: Date.now(),
              type: "modify",
              path: p,
              hash: getResp.metadata?.hash || serverMeta.hash,
              mtime: getResp.metadata?.mtime || serverMeta.mtime,
              size: getResp.metadata?.size || serverMeta.size,
              isBinary: getResp.isBinary || false,
              content: getResp.content
            });
          }
        } catch (err) {
          console.error(`[SyncClient] Failed to download ${p}:`, err);
        }
      }
      const adapter = this.app.vault.adapter;
      for (const p of diff.toUpload) {
        try {
          if (await adapter.exists(p)) {
            const isBin = (0, import_shared.isBinaryFile)(p);
            let contentStr = "";
            let hash = "";
            if (isBin) {
              const buf = await adapter.readBinary(p);
              hash = (0, import_shared.hashBuffer)(buf);
              contentStr = Buffer.from(buf).toString("base64");
            } else {
              contentStr = await adapter.read(p);
              hash = (0, import_shared.hashString)(contentStr);
            }
            const stat = await adapter.stat(p);
            await this.request({
              id: `put-${Date.now()}-${Math.random().toString(36).substring(2, 6)}`,
              type: "FILE_PUT_REQUEST",
              timestamp: Date.now(),
              path: p,
              content: contentStr,
              isBinary: isBin,
              mtime: stat?.mtime || Date.now(),
              hash
            });
            this.logActivity("upload", p, "Reconciliation: Uploaded to server");
          }
        } catch (err) {
          console.error(`[SyncClient] Failed to upload ${p}:`, err);
        }
      }
      while (this.plugin.offlineQueue.size() > 0) {
        const evt = await this.plugin.offlineQueue.dequeue();
        if (evt) {
          await this.onLocalSyncEvent(evt);
        }
      }
      this.plugin.settings.lastSyncedTimestamp = Date.now();
      await this.plugin.saveSettings();
      this.logActivity("info", "", "Reconciliation complete! Vault is in sync.");
      this.setStatus("synced");
    } catch (err) {
      console.error("[SyncClient] Error during reconciliation:", err);
      this.logActivity("error", "", `Reconciliation failed: ${err instanceof Error ? err.message : String(err)}`);
      this.setStatus("error", "Reconciliation failed");
    } finally {
      this.isReconciling = false;
    }
  }
  async buildLocalManifest() {
    const manifest = {};
    const adapter = this.app.vault.adapter;
    const scanDir = async (dir) => {
      const listing = await adapter.list(dir);
      for (const filePath of listing.files) {
        const normalized = filePath.replace(/\\/g, "/");
        if (this.plugin.ignoreFilter.isIgnored(normalized))
          continue;
        try {
          const isBin = (0, import_shared.isBinaryFile)(normalized);
          let hash = "";
          if (isBin) {
            const buf = await adapter.readBinary(normalized);
            hash = (0, import_shared.hashBuffer)(buf);
          } else {
            const text = await adapter.read(normalized);
            hash = (0, import_shared.hashString)(text);
          }
          const stat = await adapter.stat(normalized);
          manifest[normalized] = {
            path: normalized,
            hash,
            mtime: stat?.mtime || Date.now(),
            size: stat?.size || 0,
            isBinary: isBin
          };
        } catch {
        }
      }
      for (const subDir of listing.folders) {
        const normalizedFolder = subDir.replace(/\\/g, "/");
        if (!this.plugin.ignoreFilter.isIgnored(normalizedFolder)) {
          await scanDir(subDir);
        }
      }
    };
    await scanDir("");
    if (this.plugin.settings.syncObsidianConfig) {
      await scanDir(this.app.vault.configDir || ".obsidian");
    }
    return manifest;
  }
};

// src/vault-watcher.ts
var import_obsidian3 = require("obsidian");
var import_shared2 = __toESM(require_dist());
var VaultWatcher = class {
  constructor(app, plugin, ignoreFilter) {
    this.eventRefs = [];
    this.debounceTimers = /* @__PURE__ */ new Map();
    this.localSuppressions = /* @__PURE__ */ new Map();
    // path -> expiresAt
    this.obsidianConfigHashes = /* @__PURE__ */ new Map();
    // path -> last hash for .obsidian files
    this.initialConfigScanned = false;
    this.configPollInterval = null;
    this.app = app;
    this.plugin = plugin;
    this.ignoreFilter = ignoreFilter;
  }
  recordConfigHash(path, hash) {
    this.obsidianConfigHashes.set(path.replace(/\\/g, "/"), hash);
  }
  start() {
    this.eventRefs.push(
      this.app.vault.on("create", (file) => this.handleVaultCreate(file)),
      this.app.vault.on("modify", (file) => this.handleVaultModify(file)),
      this.app.vault.on("delete", (file) => this.handleVaultDelete(file)),
      this.app.vault.on("rename", (file, oldPath) => this.handleVaultRename(file, oldPath))
    );
    if (this.plugin.settings.syncObsidianConfig) {
      this.startObsidianConfigPoller();
    }
  }
  stop() {
    for (const ref of this.eventRefs) {
      this.app.vault.offref(ref);
    }
    this.eventRefs = [];
    for (const timer of this.debounceTimers.values()) {
      window.clearTimeout(timer);
    }
    this.debounceTimers.clear();
    if (this.configPollInterval !== null) {
      window.clearInterval(this.configPollInterval);
      this.configPollInterval = null;
    }
  }
  /**
   * Temporarily suppresses change detection for a path when written by sync client
   */
  suppressPath(path, durationMs = 3e3) {
    const normalized = path.replace(/\\/g, "/");
    this.localSuppressions.set(normalized, Date.now() + durationMs);
  }
  isSuppressed(path) {
    const normalized = path.replace(/\\/g, "/");
    const expiresAt = this.localSuppressions.get(normalized);
    if (!expiresAt)
      return false;
    if (Date.now() > expiresAt) {
      this.localSuppressions.delete(normalized);
      return false;
    }
    return true;
  }
  async handleVaultCreate(file) {
    if (file instanceof import_obsidian3.TFolder)
      return;
    if (this.isSuppressed(file.path) || this.ignoreFilter.isIgnored(file.path))
      return;
    await this.scheduleFileChange("create", file.path);
  }
  async handleVaultModify(file) {
    if (file instanceof import_obsidian3.TFolder)
      return;
    if (this.isSuppressed(file.path) || this.ignoreFilter.isIgnored(file.path))
      return;
    await this.scheduleFileChange("modify", file.path);
  }
  async handleVaultDelete(file) {
    if (file instanceof import_obsidian3.TFolder)
      return;
    if (this.isSuppressed(file.path) || this.ignoreFilter.isIgnored(file.path))
      return;
    const event = {
      id: `cli-${Date.now()}-${Math.random().toString(36).substring(2, 7)}`,
      clientId: this.plugin.settings.clientId,
      timestamp: Date.now(),
      type: "delete",
      path: file.path
    };
    await this.plugin.syncClient.onLocalSyncEvent(event);
  }
  async handleVaultRename(file, oldPath) {
    if (file instanceof import_obsidian3.TFolder)
      return;
    if (this.isSuppressed(file.path) || this.isSuppressed(oldPath))
      return;
    if (this.ignoreFilter.isIgnored(file.path) && this.ignoreFilter.isIgnored(oldPath))
      return;
    const isBin = (0, import_shared2.isBinaryFile)(file.path);
    let hash = "";
    let mtime = Date.now();
    if (file instanceof import_obsidian3.TFile) {
      mtime = file.stat.mtime;
      if (isBin) {
        const buf = await this.app.vault.readBinary(file);
        hash = (0, import_shared2.hashBuffer)(buf);
      } else {
        const text = await this.app.vault.read(file);
        hash = (0, import_shared2.hashString)(text);
      }
    }
    const event = {
      id: `cli-${Date.now()}-${Math.random().toString(36).substring(2, 7)}`,
      clientId: this.plugin.settings.clientId,
      timestamp: Date.now(),
      type: "rename",
      oldPath,
      newPath: file.path,
      hash,
      mtime
    };
    await this.plugin.syncClient.onLocalSyncEvent(event);
  }
  async scheduleFileChange(type, path) {
    if (this.debounceTimers.has(path)) {
      window.clearTimeout(this.debounceTimers.get(path));
    }
    const timer = window.setTimeout(async () => {
      this.debounceTimers.delete(path);
      try {
        if (this.isSuppressed(path))
          return;
        const file = this.app.vault.getAbstractFileByPath(path);
        if (!file || !(file instanceof import_obsidian3.TFile))
          return;
        const isBin = (0, import_shared2.isBinaryFile)(path);
        let content;
        let hash;
        if (isBin) {
          const buf = await this.app.vault.readBinary(file);
          content = Buffer.from(buf).toString("base64");
          hash = (0, import_shared2.hashBuffer)(buf);
        } else {
          content = await this.app.vault.read(file);
          hash = (0, import_shared2.hashString)(content);
        }
        const baseHash = this.plugin.conflictHandler.getBaseSnapshot(path);
        const event = {
          id: `cli-${Date.now()}-${Math.random().toString(36).substring(2, 7)}`,
          clientId: this.plugin.settings.clientId,
          timestamp: Date.now(),
          type,
          path,
          hash,
          mtime: file.stat.mtime,
          size: file.stat.size,
          isBinary: isBin,
          baseHash: baseHash ? (0, import_shared2.hashString)(baseHash) : void 0,
          content: content.length < 500 * 1024 ? content : void 0,
          chunked: content.length >= 500 * 1024
        };
        if (!isBin) {
          this.plugin.conflictHandler.recordBaseSnapshot(path, content);
        }
        await this.plugin.syncClient.onLocalSyncEvent(event, content);
      } catch (err) {
        console.error(`[VaultWatcher] Failed to process change on ${path}:`, err);
      }
    }, this.plugin.settings.debounceMs);
    this.debounceTimers.set(path, timer);
  }
  /**
   * Scans .obsidian configuration folder periodically using vault adapter
   */
  startObsidianConfigPoller() {
    if (this.configPollInterval !== null)
      return;
    this.configPollInterval = window.setInterval(async () => {
      await this.scanObsidianConfigFolder();
    }, 2500);
    this.scanObsidianConfigFolder();
  }
  async scanObsidianConfigFolder() {
    if (!this.plugin.settings.syncObsidianConfig)
      return;
    try {
      const adapter = this.app.vault.adapter;
      const configDir = this.app.vault.configDir || ".obsidian";
      const seenConfigPaths = /* @__PURE__ */ new Set();
      const scanSubDir = async (dir) => {
        const listing = await adapter.list(dir);
        for (const filePath of listing.files) {
          const normalized = filePath.replace(/\\/g, "/");
          if (this.ignoreFilter.isIgnored(normalized))
            continue;
          if (this.isSuppressed(normalized))
            continue;
          seenConfigPaths.add(normalized);
          try {
            const isBin = (0, import_shared2.isBinaryFile)(normalized);
            let contentStr;
            let currentHash;
            if (isBin) {
              const buf = await adapter.readBinary(normalized);
              currentHash = (0, import_shared2.hashBuffer)(buf);
              contentStr = Buffer.from(buf).toString("base64");
            } else {
              contentStr = await adapter.read(normalized);
              currentHash = (0, import_shared2.hashString)(contentStr);
            }
            const previousHash = this.obsidianConfigHashes.get(normalized);
            if (previousHash !== void 0 && previousHash !== currentHash) {
              this.obsidianConfigHashes.set(normalized, currentHash);
              const stat = await adapter.stat(normalized);
              const event = {
                id: `cli-${Date.now()}-${Math.random().toString(36).substring(2, 7)}`,
                clientId: this.plugin.settings.clientId,
                timestamp: Date.now(),
                type: "modify",
                path: normalized,
                hash: currentHash,
                mtime: stat?.mtime || Date.now(),
                size: stat?.size || contentStr.length,
                isBinary: isBin,
                content: contentStr
              };
              await this.plugin.syncClient.onLocalSyncEvent(event, contentStr);
            } else if (previousHash === void 0) {
              this.obsidianConfigHashes.set(normalized, currentHash);
              if (this.initialConfigScanned) {
                const stat = await adapter.stat(normalized);
                const event = {
                  id: `cli-${Date.now()}-${Math.random().toString(36).substring(2, 7)}`,
                  clientId: this.plugin.settings.clientId,
                  timestamp: Date.now(),
                  type: "create",
                  path: normalized,
                  hash: currentHash,
                  mtime: stat?.mtime || Date.now(),
                  size: stat?.size || contentStr.length,
                  isBinary: isBin,
                  content: contentStr
                };
                await this.plugin.syncClient.onLocalSyncEvent(event, contentStr);
              }
            }
          } catch {
          }
        }
        for (const subDir of listing.folders) {
          const normalizedFolder = subDir.replace(/\\/g, "/");
          if (!this.ignoreFilter.isIgnored(normalizedFolder)) {
            await scanSubDir(subDir);
          }
        }
      };
      await scanSubDir(configDir);
      if (this.initialConfigScanned) {
        for (const [trackedPath] of this.obsidianConfigHashes.entries()) {
          if (!seenConfigPaths.has(trackedPath) && !this.isSuppressed(trackedPath)) {
            this.obsidianConfigHashes.delete(trackedPath);
            const deleteEvent = {
              id: `cli-${Date.now()}-${Math.random().toString(36).substring(2, 7)}`,
              clientId: this.plugin.settings.clientId,
              timestamp: Date.now(),
              type: "delete",
              path: trackedPath
            };
            await this.plugin.syncClient.onLocalSyncEvent(deleteEvent);
          }
        }
      }
      this.initialConfigScanned = true;
    } catch (err) {
      console.error("[VaultWatcher] Error scanning .obsidian folder:", err);
    }
  }
};

// src/conflict-handler.ts
var import_obsidian4 = require("obsidian");
var import_shared3 = __toESM(require_dist());
var ConflictHandler = class {
  // path -> content snapshot when last synced
  constructor(app) {
    this.baseSnapshots = /* @__PURE__ */ new Map();
    this.app = app;
  }
  recordBaseSnapshot(path, content) {
    this.baseSnapshots.set(path, content);
  }
  getBaseSnapshot(path) {
    return this.baseSnapshots.get(path) || "";
  }
  removeBaseSnapshot(path) {
    this.baseSnapshots.delete(path);
  }
  /**
   * Resolves a potentially conflicting incoming file update.
   */
  async resolveTextConflict(filePath, currentLocalText, incomingRemoteText) {
    const baseText = this.getBaseSnapshot(filePath);
    const mergeResult = (0, import_shared3.threeWayMerge)(
      baseText,
      currentLocalText,
      incomingRemoteText,
      filePath
    );
    if (!mergeResult.hasConflict) {
      this.recordBaseSnapshot(filePath, mergeResult.mergedText);
      return {
        textToWrite: mergeResult.mergedText,
        conflictOccurred: false
      };
    }
    const conflictPath = (0, import_shared3.generateConflictPath)(filePath);
    try {
      await this.app.vault.create(conflictPath, incomingRemoteText);
      new import_obsidian4.Notice(`\u26A0\uFE0F Sync Conflict on "${filePath}". Saved incoming copy as "${conflictPath}".`, 8e3);
    } catch (err) {
      console.error(`[ConflictHandler] Failed to write conflict file ${conflictPath}:`, err);
    }
    this.recordBaseSnapshot(filePath, currentLocalText);
    return {
      textToWrite: currentLocalText,
      conflictOccurred: true,
      conflictFileCreated: conflictPath
    };
  }
};

// src/offline-queue.ts
var OfflineQueue = class {
  constructor(initialQueue = [], saveCallback) {
    this.queue = [];
    this.queue = [...initialQueue];
    this.saveCallback = saveCallback;
  }
  getEvents() {
    return [...this.queue];
  }
  size() {
    return this.queue.length;
  }
  async enqueue(event) {
    const targetPath = "path" in event ? event.path : "newPath" in event ? event.newPath : "";
    const existingIndex = this.queue.findIndex((e) => {
      const p = "path" in e ? e.path : "newPath" in e ? e.newPath : "";
      return p === targetPath;
    });
    if (existingIndex !== -1) {
      this.queue[existingIndex] = event;
    } else {
      this.queue.push(event);
    }
    await this.saveCallback();
  }
  async dequeue() {
    const item = this.queue.shift();
    if (item) {
      await this.saveCallback();
    }
    return item;
  }
  async clear() {
    this.queue = [];
    await this.saveCallback();
  }
  serialize() {
    return this.queue;
  }
};

// src/status-view.ts
var import_obsidian5 = require("obsidian");
var StatusBarItemView = class {
  constructor(el, plugin) {
    this.el = el;
    this.plugin = plugin;
    this.init();
  }
  init() {
    this.el.addClass("vps-sync-status-bar");
    this.el.setAttribute("aria-label", "VPS Live Sync: Initializing");
    this.updateStatus("disconnected");
    this.el.addEventListener("click", () => {
      this.plugin.openActivityLogModal();
    });
  }
  updateStatus(status, detail) {
    this.el.empty();
    const iconSpan = this.el.createSpan({ cls: "vps-sync-icon" });
    const textSpan = this.el.createSpan({ cls: "vps-sync-text" });
    let iconName = "sync";
    let label = "VPS Sync";
    let tooltip = "VPS Live Sync";
    switch (status) {
      case "synced":
        iconName = "check-circle";
        label = "Synced";
        tooltip = "VPS Sync: All notes up to date";
        this.el.removeClass("is-syncing", "is-error", "is-disconnected");
        this.el.addClass("is-synced");
        break;
      case "syncing":
      case "reconciling":
        iconName = "refresh-cw";
        label = status === "reconciling" ? "Reconciling..." : "Syncing...";
        tooltip = `VPS Sync: ${status === "reconciling" ? "Checking for changes" : "Transferring updates"}`;
        this.el.removeClass("is-synced", "is-error", "is-disconnected");
        this.el.addClass("is-syncing");
        break;
      case "connecting":
      case "authenticating":
        iconName = "loader";
        label = "Connecting...";
        tooltip = "VPS Sync: Connecting to VPS server";
        this.el.removeClass("is-synced", "is-error", "is-disconnected");
        this.el.addClass("is-syncing");
        break;
      case "error":
        iconName = "alert-triangle";
        label = "Sync Error";
        tooltip = `VPS Sync Error: ${detail || "Check settings"}`;
        this.el.removeClass("is-synced", "is-syncing", "is-disconnected");
        this.el.addClass("is-error");
        break;
      case "disconnected":
      default:
        iconName = "cloud-off";
        label = "Disconnected";
        tooltip = `VPS Sync: Disconnected (${detail || "Offline"})`;
        this.el.removeClass("is-synced", "is-syncing", "is-error");
        this.el.addClass("is-disconnected");
        break;
    }
    (0, import_obsidian5.setIcon)(iconSpan, iconName);
    textSpan.setText(label);
    this.el.setAttribute("aria-label", tooltip);
  }
};
var ActivityLogModal = class extends import_obsidian5.Modal {
  constructor(app, plugin) {
    super(app);
    this.plugin = plugin;
  }
  onOpen() {
    const { contentEl } = this;
    contentEl.empty();
    contentEl.addClass("vps-sync-log-modal");
    contentEl.createEl("h2", { text: "VPS Sync Activity Log" });
    const headerBar = contentEl.createDiv({ cls: "vps-sync-modal-header" });
    const statusBadge = headerBar.createSpan({
      cls: `vps-sync-badge vps-sync-badge-${this.plugin.syncClient.getStatus()}`,
      text: `Status: ${this.plugin.syncClient.getStatus().toUpperCase()}`
    });
    const btnContainer = headerBar.createDiv({ cls: "vps-sync-header-actions" });
    const syncBtn = btnContainer.createEl("button", { text: "Sync Now", cls: "mod-cta" });
    syncBtn.addEventListener("click", async () => {
      syncBtn.disabled = true;
      syncBtn.setText("Syncing...");
      await this.plugin.syncClient.reconcileVault();
      syncBtn.disabled = false;
      syncBtn.setText("Sync Now");
      this.renderLogList(logContainer);
    });
    const reconnectBtn = btnContainer.createEl("button", { text: "Reconnect" });
    reconnectBtn.addEventListener("click", () => {
      this.plugin.syncClient.reconnect();
      setTimeout(() => this.renderLogList(logContainer), 1e3);
    });
    const logContainer = contentEl.createDiv({ cls: "vps-sync-log-container" });
    this.renderLogList(logContainer);
  }
  renderLogList(container) {
    container.empty();
    const logs = this.plugin.syncClient.getLogs();
    if (logs.length === 0) {
      container.createEl("p", {
        text: "No sync activity recorded yet. Edits and sync events will appear here.",
        cls: "vps-sync-empty-log"
      });
      return;
    }
    const table = container.createEl("table", { cls: "vps-sync-log-table" });
    const thead = table.createEl("thead");
    const headerRow = thead.createEl("tr");
    headerRow.createEl("th", { text: "Time" });
    headerRow.createEl("th", { text: "Type" });
    headerRow.createEl("th", { text: "File Path" });
    headerRow.createEl("th", { text: "Details" });
    const tbody = table.createEl("tbody");
    for (const log of logs) {
      const row = tbody.createEl("tr", { cls: `vps-log-row-${log.type}` });
      const timeStr = new Date(log.timestamp).toLocaleTimeString();
      row.createEl("td", { text: timeStr, cls: "vps-log-time" });
      const typeTd = row.createEl("td", { cls: "vps-log-type" });
      typeTd.createSpan({ cls: `vps-badge-${log.type}`, text: log.type.toUpperCase() });
      row.createEl("td", { text: log.path || "\u2014", cls: "vps-log-path" });
      row.createEl("td", { text: log.message, cls: "vps-log-msg" });
    }
  }
  onClose() {
    const { contentEl } = this;
    contentEl.empty();
  }
};

// src/main.ts
var import_shared4 = __toESM(require_dist());
var VPSVaultSyncPlugin = class extends import_obsidian6.Plugin {
  constructor() {
    super(...arguments);
    this.settings = DEFAULT_SETTINGS;
  }
  async onload() {
    console.log("[VPS Vault Sync] Loading plugin...");
    await this.loadPluginData();
    if (!this.settings.clientId) {
      this.settings.clientId = `client-${Date.now()}-${Math.random().toString(36).substring(2, 8)}`;
      await this.saveSettings();
    }
    this.ignoreFilter = new import_shared4.IgnoreFilter({
      syncObsidianConfig: this.settings.syncObsidianConfig,
      syncWorkspace: this.settings.syncWorkspace
    });
    this.conflictHandler = new ConflictHandler(this.app);
    this.offlineQueue = new OfflineQueue(
      [],
      async () => {
        await this.savePluginData();
      }
    );
    this.syncClient = new SyncClient(this.app, this);
    this.vaultWatcher = new VaultWatcher(this.app, this, this.ignoreFilter);
    const statusBarEl = this.addStatusBarItem();
    this.statusBarItemView = new StatusBarItemView(statusBarEl, this);
    this.syncClient.onStatusChange((status, detail) => {
      this.statusBarItemView.updateStatus(status, detail);
    });
    this.addRibbonIcon("refresh-cw", "VPS Live Sync: Open Activity Log", () => {
      this.openActivityLogModal();
    });
    this.addCommand({
      id: "vps-sync-reconcile-now",
      name: "Force Vault Sync Now",
      callback: async () => {
        new import_obsidian6.Notice("Starting full vault reconciliation...");
        await this.syncClient.reconcileVault();
      }
    });
    this.addCommand({
      id: "vps-sync-view-activity-log",
      name: "View Sync Activity Log",
      callback: () => {
        this.openActivityLogModal();
      }
    });
    this.addCommand({
      id: "vps-sync-reconnect",
      name: "Reconnect to VPS Server",
      callback: () => {
        new import_obsidian6.Notice("Reconnecting to VPS sync server...");
        this.syncClient.reconnect();
      }
    });
    this.addSettingTab(new SyncSettingTab(this.app, this));
    this.vaultWatcher.start();
    if (this.settings.liveSyncEnabled && this.settings.serverUrl && this.settings.authToken) {
      this.app.workspace.onLayoutReady(() => {
        this.syncClient.connect();
      });
    }
    console.log("[VPS Vault Sync] Plugin initialized successfully.");
  }
  onunload() {
    console.log("[VPS Vault Sync] Unloading plugin...");
    this.vaultWatcher?.stop();
    this.syncClient?.disconnect();
  }
  openActivityLogModal() {
    new ActivityLogModal(this.app, this).open();
  }
  async loadPluginData() {
    const rawData = await this.loadData();
    if (rawData) {
      this.settings = Object.assign({}, DEFAULT_SETTINGS, rawData.settings || rawData);
    } else {
      this.settings = Object.assign({}, DEFAULT_SETTINGS);
    }
  }
  async saveSettings() {
    if (this.ignoreFilter) {
      this.ignoreFilter.reloadPatterns();
    }
    await this.savePluginData();
  }
  async savePluginData() {
    const dataToSave = {
      settings: this.settings,
      offlineQueue: this.offlineQueue ? this.offlineQueue.serialize() : []
    };
    await this.saveData(dataToSave);
  }
};
