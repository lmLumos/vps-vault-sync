{
  "name": "@vps-vault-sync/shared",
  "version": "1.0.0",
  "description": "Shared types, protocol definitions, and merge algorithms for VPS Vault Sync",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "node --test 'dist/**/*.test.js'"
  },
  "dependencies": {
    "diff-match-patch": "^1.0.5"
  },
  "devDependencies": {
    "@types/diff-match-patch": "^1.0.36",
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  }
}
